# TODO

<!-- ADR-GENERATED-TASKS -->

<!-- This section is managed by `generate_adr_todo`. Tasks outside this
     bounded block are preserved verbatim across re-runs. Toggling Status -->
<!-- generated-at: 2026-04-30T16:50:36.675Z -->

## ADR-Generated Tasks

## Write tests for: Implement decision: Architectural Decision Records (ADRs)

<!-- task-id: readme-md/implement-decision-architectural-decision-records-adrs/test -->
<!-- adr: README.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** medium
**Category:** #### Foundation Architecture (ADRs 001-002)

## Implement decision: Architectural Decision Records (ADRs)

<!-- task-id: readme-md/implement-decision-architectural-decision-records-adrs/production -->
<!-- adr: README.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** medium
**Category:** #### Foundation Architecture (ADRs 001-002)

## Write tests for: Implement decision: ADR-001: MCP Server Architecture using TypeScript SDK

<!-- task-id: adr-0001-mcp-server-architecture-md/implement-decision-adr-001-mcp-server-architecture-using-typescript-sdk/test -->
<!-- adr: adr-0001-mcp-server-architecture.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high

We will implement the DocuMCP server using the TypeScript Model Context Protocol SDK, following a modular, stateless architecture pattern.

## Implement decision: ADR-001: MCP Server Architecture using TypeScript SDK

<!-- task-id: adr-0001-mcp-server-architecture-md/implement-decision-adr-001-mcp-server-architecture-using-typescript-sdk/production -->
<!-- adr: adr-0001-mcp-server-architecture.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high

We will implement the DocuMCP server using the TypeScript Model Context Protocol SDK, following a modular, stateless architecture pattern.

## Write tests for: Implement decision: ADR-002: Multi-Layered Repository Analysis Engine Design

<!-- task-id: adr-0002-repository-analysis-engine-md/implement-decision-adr-002-multi-layered-repository-analysis-engine-design/test -->
<!-- adr: adr-0002-repository-analysis-engine.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#118

We will implement a multi-layered repository analysis engine that examines repositories from multiple perspectives to build comprehensive project profiles.

## Implement decision: ADR-002: Multi-Layered Repository Analysis Engine Design

<!-- task-id: adr-0002-repository-analysis-engine-md/implement-decision-adr-002-multi-layered-repository-analysis-engine-design/production -->
<!-- adr: adr-0002-repository-analysis-engine.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#118

We will implement a multi-layered repository analysis engine that examines repositories from multiple perspectives to build comprehensive project profiles.

## Write tests for: Implement decision: ADR-003: Static Site Generator Recommendation Engine Design

<!-- task-id: adr-0003-static-site-generator-recommendation-engine-md/implement-decision-adr-003-static-site-generator-recommendation-engine-design/test -->
<!-- adr: adr-0003-static-site-generator-recommendation-engine.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** medium

We will implement a multi-criteria decision analysis (MCDA) framework that evaluates project characteristics against SSG capabilities to provide ranked recommendations with confidence scores and detailed justifications.

## Implement decision: ADR-003: Static Site Generator Recommendation Engine Design

<!-- task-id: adr-0003-static-site-generator-recommendation-engine-md/implement-decision-adr-003-static-site-generator-recommendation-engine-design/production -->
<!-- adr: adr-0003-static-site-generator-recommendation-engine.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** medium

We will implement a multi-criteria decision analysis (MCDA) framework that evaluates project characteristics against SSG capabilities to provide ranked recommendations with confidence scores and detailed justifications.

## Write tests for: Implement decision: ADR-004: Diataxis Framework Integration for Documentation Structure

<!-- task-id: adr-0004-diataxis-framework-integration-md/implement-decision-adr-004-diataxis-framework-integration-for-documentation-stru/test -->
<!-- adr: adr-0004-diataxis-framework-integration.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** medium
**Related issues:** tosin2013/documcp#119

We will integrate the Diataxis framework as the foundational information architecture for all DocuMCP-generated documentation structures, with intelligent content planning and navigation generation adapted to each static site generator's capabilities.

## Implement decision: ADR-004: Diataxis Framework Integration for Documentation Structure

<!-- task-id: adr-0004-diataxis-framework-integration-md/implement-decision-adr-004-diataxis-framework-integration-for-documentation-stru/production -->
<!-- adr: adr-0004-diataxis-framework-integration.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** medium
**Related issues:** tosin2013/documcp#119

We will integrate the Diataxis framework as the foundational information architecture for all DocuMCP-generated documentation structures, with intelligent content planning and navigation generation adapted to each static site generator's capabilities.

## Write tests for: Implement decision: ADR-005: GitHub Pages Deployment Automation Architecture

<!-- task-id: adr-0005-github-pages-deployment-automation-md/implement-decision-adr-005-github-pages-deployment-automation-architecture/test -->
<!-- adr: adr-0005-github-pages-deployment-automation.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high

We will implement a comprehensive GitHub Pages deployment orchestration system that generates optimized, SSG-specific GitHub Actions workflows with intelligent configuration, error handling, and verification capabilities.

## Implement decision: ADR-005: GitHub Pages Deployment Automation Architecture

<!-- task-id: adr-0005-github-pages-deployment-automation-md/implement-decision-adr-005-github-pages-deployment-automation-architecture/production -->
<!-- adr: adr-0005-github-pages-deployment-automation.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high

We will implement a comprehensive GitHub Pages deployment orchestration system that generates optimized, SSG-specific GitHub Actions workflows with intelligent configuration, error handling, and verification capabilities.

## Write tests for: Implement decision: ADR-006: MCP Tools API Design and Interface Specification

<!-- task-id: adr-0006-mcp-tools-api-design-md/implement-decision-adr-006-mcp-tools-api-design-and-interface-specification/test -->
<!-- adr: adr-0006-mcp-tools-api-design.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** medium

We will implement a comprehensive MCP Tools API consisting of six core tools that cover the complete documentation deployment workflow, with additional utility tools for advanced scenarios and troubleshooting.

## Implement decision: ADR-006: MCP Tools API Design and Interface Specification

<!-- task-id: adr-0006-mcp-tools-api-design-md/implement-decision-adr-006-mcp-tools-api-design-and-interface-specification/production -->
<!-- adr: adr-0006-mcp-tools-api-design.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** medium

We will implement a comprehensive MCP Tools API consisting of six core tools that cover the complete documentation deployment workflow, with additional utility tools for advanced scenarios and troubleshooting.

## Write tests for: Implement `ListPromptsRequestSchema` and `GetPromptRequestSchema` handlers

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/implement-listpromptsrequestschema-and-getpromptrequestschema-handlers/test -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Implement `ListPromptsRequestSchema` and `GetPromptRequestSchema` handlers

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/implement-listpromptsrequestschema-and-getpromptrequestschema-handlers/production -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Write tests for: Implement `ListResourcesRequestSchema` and `ReadResourceRequestSchema` handlers

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/implement-listresourcesrequestschema-and-readresourcerequestschema-handlers/test -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Implement `ListResourcesRequestSchema` and `ReadResourceRequestSchema` handlers

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/implement-listresourcesrequestschema-and-readresourcerequestschema-handlers/production -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Write tests for: Create resource URI schema and routing system

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/create-resource-uri-schema-and-routing-system/test -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Create resource URI schema and routing system

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/create-resource-uri-schema-and-routing-system/production -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Write tests for: Add MCP capabilities registration for prompts and resources

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/add-mcp-capabilities-registration-for-prompts-and-resources/test -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Add MCP capabilities registration for prompts and resources

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/add-mcp-capabilities-registration-for-prompts-and-resources/production -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Write tests for: Create "analyze-and-recommend" workflow prompt

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/create-analyze-and-recommend-workflow-prompt/test -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Create "analyze-and-recommend" workflow prompt

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/create-analyze-and-recommend-workflow-prompt/production -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Write tests for: Create "setup-documentation" structure prompt

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/create-setup-documentation-structure-prompt/test -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Create "setup-documentation" structure prompt

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/create-setup-documentation-structure-prompt/production -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Write tests for: Create "troubleshoot-deployment" diagnostic prompt

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/create-troubleshoot-deployment-diagnostic-prompt/test -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Create "troubleshoot-deployment" diagnostic prompt

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/create-troubleshoot-deployment-diagnostic-prompt/production -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Write tests for: Add prompt argument validation and help text

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/add-prompt-argument-validation-and-help-text/test -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Add prompt argument validation and help text

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/add-prompt-argument-validation-and-help-text/production -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Write tests for: Implement resource caching for analysis results

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/implement-resource-caching-for-analysis-results/test -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Implement resource caching for analysis results

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/implement-resource-caching-for-analysis-results/production -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Write tests for: Add generated configuration file resources

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/add-generated-configuration-file-resources/test -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Add generated configuration file resources

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/add-generated-configuration-file-resources/production -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Write tests for: Create template library resources

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/create-template-library-resources/test -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Create template library resources

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/create-template-library-resources/production -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Write tests for: Add resource cleanup and lifecycle management

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/add-resource-cleanup-and-lifecycle-management/test -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Add resource cleanup and lifecycle management

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/add-resource-cleanup-and-lifecycle-management/production -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Write tests for: Dynamic prompt generation based on project characteristics

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/dynamic-prompt-generation-based-on-project-characteristics/test -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Dynamic prompt generation based on project characteristics

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/dynamic-prompt-generation-based-on-project-characteristics/production -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Write tests for: Contextual resource recommendations

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/contextual-resource-recommendations/test -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Contextual resource recommendations

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/contextual-resource-recommendations/production -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Write tests for: Prompt composition for complex workflows

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/prompt-composition-for-complex-workflows/test -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Prompt composition for complex workflows

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/prompt-composition-for-complex-workflows/production -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Write tests for: Integration testing with major MCP clients

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/integration-testing-with-major-mcp-clients/test -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Integration testing with major MCP clients

<!-- task-id: adr-0007-mcp-prompts-and-resources-integration-md/integration-testing-with-major-mcp-clients/production -->
<!-- adr: adr-0007-mcp-prompts-and-resources-integration.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high

We will implement AI assistance using MCP's native **Prompts** and **Resources** capabilities, providing pre-written prompt templates for documentation workflows and exposing generated content through the MCP resource system.

## Write tests for: Implement decision: ADR-008: Intelligent Content Population Engine for Diataxis Documentation

<!-- task-id: adr-0008-intelligent-content-population-engine-md/implement-decision-adr-008-intelligent-content-population-engine-for-diataxis-do/test -->
<!-- adr: adr-0008-intelligent-content-population-engine.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** medium

We will implement an Intelligent Content Population Engine that bridges repository analysis with Diataxis content generation, creating the missing layer between structural generation and user-ready documentation.

## Implement decision: ADR-008: Intelligent Content Population Engine for Diataxis Documentation

<!-- task-id: adr-0008-intelligent-content-population-engine-md/implement-decision-adr-008-intelligent-content-population-engine-for-diataxis-do/production -->
<!-- adr: adr-0008-intelligent-content-population-engine.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** medium

We will implement an Intelligent Content Population Engine that bridges repository analysis with Diataxis content generation, creating the missing layer between structural generation and user-ready documentation.

## Write tests for: Implement decision: ADR-009: Content Accuracy and Validation Framework for Generated Documentation

<!-- task-id: adr-0009-content-accuracy-validation-framework-md/implement-decision-adr-009-content-accuracy-and-validation-framework-for-generat/test -->
<!-- adr: adr-0009-content-accuracy-validation-framework.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** medium
**Related issues:** tosin2013/documcp#111

We will implement a comprehensive Content Accuracy and Validation Framework that treats content correctness as a first-class architectural concern, with systematic approaches to uncertainty management, reality verification, and continuous accuracy improvement.

## Implement decision: ADR-009: Content Accuracy and Validation Framework for Generated Documentation

<!-- task-id: adr-0009-content-accuracy-validation-framework-md/implement-decision-adr-009-content-accuracy-and-validation-framework-for-generat/production -->
<!-- adr: adr-0009-content-accuracy-validation-framework.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** medium
**Related issues:** tosin2013/documcp#111

We will implement a comprehensive Content Accuracy and Validation Framework that treats content correctness as a first-class architectural concern, with systematic approaches to uncertainty management, reality verification, and continuous accuracy improvement.

## Write tests for: Implement decision: ADR-010: MCP Resource Pattern Redesign

<!-- task-id: adr-0010-mcp-resource-pattern-redesign-md/implement-decision-adr-010-mcp-resource-pattern-redesign/test -->
<!-- adr: adr-0010-mcp-resource-pattern-redesign.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** medium
**Category:** \*\*

## Implement decision: ADR-010: MCP Resource Pattern Redesign

<!-- task-id: adr-0010-mcp-resource-pattern-redesign-md/implement-decision-adr-010-mcp-resource-pattern-redesign/production -->
<!-- adr: adr-0010-mcp-resource-pattern-redesign.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** medium
**Category:** \*\*

## Write tests for: Implement decision: ADR-011: Code Execution with MCP (CE-MCP) Compatibility

<!-- task-id: adr-0011-ce-mcp-compatibility-md/implement-decision-adr-011-code-execution-with-mcp-ce-mcp-compatibility/test -->
<!-- adr: adr-0011-ce-mcp-compatibility.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** medium

**documcp is already CE-MCP compatible** without requiring architectural changes. Our existing stateless, tool-based architecture aligns perfectly with Code Mode requirements.

## Implement decision: ADR-011: Code Execution with MCP (CE-MCP) Compatibility

<!-- task-id: adr-0011-ce-mcp-compatibility-md/implement-decision-adr-011-code-execution-with-mcp-ce-mcp-compatibility/production -->
<!-- adr: adr-0011-ce-mcp-compatibility.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** medium

**documcp is already CE-MCP compatible** without requiring architectural changes. Our existing stateless, tool-based architecture aligns perfectly with Code Mode requirements.

## Write tests for: Implement decision: ADR-012: Priority Scoring System for Documentation Drift Detection

<!-- task-id: adr-0012-priority-scoring-system-for-documentation-drift-md/implement-decision-adr-012-priority-scoring-system-for-documentation-drift-detec/test -->
<!-- adr: adr-0012-priority-scoring-system-for-documentation-drift.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#113, tosin2013/documcp#114, tosin2013/documcp#125

We will implement a multi-factor priority scoring system for documentation drift that considers code complexity, usage frequency, change magnitude, documentation coverage, staleness, and user feedback. The system calculates an overall priority score (0-100) and provides actionable recommendations (critical/high/medium/low) to guide developers in addressing the most impactful drift issues first.

## Implement decision: ADR-012: Priority Scoring System for Documentation Drift Detection

<!-- task-id: adr-0012-priority-scoring-system-for-documentation-drift-md/implement-decision-adr-012-priority-scoring-system-for-documentation-drift-detec/production -->
<!-- adr: adr-0012-priority-scoring-system-for-documentation-drift.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#113, tosin2013/documcp#114, tosin2013/documcp#125

We will implement a multi-factor priority scoring system for documentation drift that considers code complexity, usage frequency, change magnitude, documentation coverage, staleness, and user feedback. The system calculates an overall priority score (0-100) and provides actionable recommendations (critical/high/medium/low) to guide developers in addressing the most impactful drift issues first.

## Write tests for: Implement decision: ADR-013: Release Pipeline and Package Distribution Architecture

<!-- task-id: adr-0013-release-pipeline-and-package-distribution-md/implement-decision-adr-013-release-pipeline-and-package-distribution-architectur/test -->
<!-- adr: adr-0013-release-pipeline-and-package-distribution.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#115, tosin2013/documcp#116, tosin2013/documcp#117

We will implement an automated release pipeline with npm package publishing, conventional commits enforcement via commitlint, automated changelog generation using standard-version, and comprehensive verification steps. The pipeline will include publication verification, error handling, retry mechanisms, and quality gates for test coverage and commit message standards.

## Implement decision: ADR-013: Release Pipeline and Package Distribution Architecture

<!-- task-id: adr-0013-release-pipeline-and-package-distribution-md/implement-decision-adr-013-release-pipeline-and-package-distribution-architectur/production -->
<!-- adr: adr-0013-release-pipeline-and-package-distribution.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#115, tosin2013/documcp#116, tosin2013/documcp#117

We will implement an automated release pipeline with npm package publishing, conventional commits enforcement via commitlint, automated changelog generation using standard-version, and comprehensive verification steps. The pipeline will include publication verification, error handling, retry mechanisms, and quality gates for test coverage and commit message standards.

## Write tests for: **Delete** `src/utils/llm-client.ts`, `src/utils/execution-simulator.ts`, `src/tools/simulate-execution.ts`, `tests/utils/llm-client.test.ts`, and the `docs/how-to/llm-integration.md` how-to.

<!-- task-id: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling-md/delete-src-utils-llm-client-ts-src-utils-execution-simulator-ts-src-tools-simula/test -->
<!-- adr: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#106, tosin2013/documcp#107, tosin2013/documcp#108, tosin2013/documcp#109, tosin2013/documcp#110

Remove the bundled LLM client and all server-side LLM execution paths from DocuMCP. Specifically:

1. **Delete** `src/utils/llm-client.ts`, `src/utils/execution-simulator.ts`, `src/tools/simulate-execution.ts`, `tests/utils/llm-client.test.ts`, and the `docs/how-to/llm-integration.md` how-to.
2. **Refactor** `src/utils/semantic-analyzer.ts` to AST-only mode: remove `useLLM`, `confidenceThreshold`, and `llmConfig` options from `createSemanticAnalyzer()`; `analysisMode` always returns `'ast'`; the `SemanticAnalyzer` public API stays compatible where reasonable.
3. **Drop** `DOCUMCP_LLM_PROVIDER`, `DOCUMCP_LLM_API_KEY`, `DOCUMCP_LLM_MODEL`, and `DOCUMCP_LLM_BASE_URL` parsing from `src/index.ts`. Emit a one-version deprecation warning before deleting the parsing logic to give downstream users a clear signal.
4. **Remove** the `simulate_execution` tool entry from the `TOOLS` array and `CallToolRequestSchema` switch in `src/index.ts`.
5. **Mark** Phase 2.5 of ADR-009 as **Superseded** by this ADR. The rest of ADR-009 (the broader content-accuracy framework) remains Accepted.
6. **Defer** any future LLM-driven semantic analysis to **MCP Sampling**. When DocuMCP genuinely needs an LLM completion (for example, context-aware example generation that AST cannot reach), it will issue a `sampling/createMessage` request through the host so the host's LLM, policy, and quotas apply uniformly.
7. **Ship** under semver as **v0.6.0** with a `BREAKING CHANGE:` footer in [CHANGELOG.md](../../CHANGELOG.md) and a clear migration note for hybrid-mode users.

## **Delete** `src/utils/llm-client.ts`, `src/utils/execution-simulator.ts`, `src/tools/simulate-execution.ts`, `tests/utils/llm-client.test.ts`, and the `docs/how-to/llm-integration.md` how-to.

<!-- task-id: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling-md/delete-src-utils-llm-client-ts-src-utils-execution-simulator-ts-src-tools-simula/production -->
<!-- adr: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#106, tosin2013/documcp#107, tosin2013/documcp#108, tosin2013/documcp#109, tosin2013/documcp#110

Remove the bundled LLM client and all server-side LLM execution paths from DocuMCP. Specifically:

1. **Delete** `src/utils/llm-client.ts`, `src/utils/execution-simulator.ts`, `src/tools/simulate-execution.ts`, `tests/utils/llm-client.test.ts`, and the `docs/how-to/llm-integration.md` how-to.
2. **Refactor** `src/utils/semantic-analyzer.ts` to AST-only mode: remove `useLLM`, `confidenceThreshold`, and `llmConfig` options from `createSemanticAnalyzer()`; `analysisMode` always returns `'ast'`; the `SemanticAnalyzer` public API stays compatible where reasonable.
3. **Drop** `DOCUMCP_LLM_PROVIDER`, `DOCUMCP_LLM_API_KEY`, `DOCUMCP_LLM_MODEL`, and `DOCUMCP_LLM_BASE_URL` parsing from `src/index.ts`. Emit a one-version deprecation warning before deleting the parsing logic to give downstream users a clear signal.
4. **Remove** the `simulate_execution` tool entry from the `TOOLS` array and `CallToolRequestSchema` switch in `src/index.ts`.
5. **Mark** Phase 2.5 of ADR-009 as **Superseded** by this ADR. The rest of ADR-009 (the broader content-accuracy framework) remains Accepted.
6. **Defer** any future LLM-driven semantic analysis to **MCP Sampling**. When DocuMCP genuinely needs an LLM completion (for example, context-aware example generation that AST cannot reach), it will issue a `sampling/createMessage` request through the host so the host's LLM, policy, and quotas apply uniformly.
7. **Ship** under semver as **v0.6.0** with a `BREAKING CHANGE:` footer in [CHANGELOG.md](../../CHANGELOG.md) and a clear migration note for hybrid-mode users.

## Write tests for: **Refactor** `src/utils/semantic-analyzer.ts` to AST-only mode: remove `useLLM`, `confidenceThreshold`, and `llmConfig` options from `createSemanticAnalyzer()`; `analysisMode` always returns `'ast'`; the `SemanticAnalyzer` public API stays compatible where reasonable.

<!-- task-id: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling-md/refactor-src-utils-semantic-analyzer-ts-to-ast-only-mode-remove-usellm-confidenc/test -->
<!-- adr: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#106, tosin2013/documcp#107, tosin2013/documcp#108, tosin2013/documcp#109, tosin2013/documcp#110

Remove the bundled LLM client and all server-side LLM execution paths from DocuMCP. Specifically:

1. **Delete** `src/utils/llm-client.ts`, `src/utils/execution-simulator.ts`, `src/tools/simulate-execution.ts`, `tests/utils/llm-client.test.ts`, and the `docs/how-to/llm-integration.md` how-to.
2. **Refactor** `src/utils/semantic-analyzer.ts` to AST-only mode: remove `useLLM`, `confidenceThreshold`, and `llmConfig` options from `createSemanticAnalyzer()`; `analysisMode` always returns `'ast'`; the `SemanticAnalyzer` public API stays compatible where reasonable.
3. **Drop** `DOCUMCP_LLM_PROVIDER`, `DOCUMCP_LLM_API_KEY`, `DOCUMCP_LLM_MODEL`, and `DOCUMCP_LLM_BASE_URL` parsing from `src/index.ts`. Emit a one-version deprecation warning before deleting the parsing logic to give downstream users a clear signal.
4. **Remove** the `simulate_execution` tool entry from the `TOOLS` array and `CallToolRequestSchema` switch in `src/index.ts`.
5. **Mark** Phase 2.5 of ADR-009 as **Superseded** by this ADR. The rest of ADR-009 (the broader content-accuracy framework) remains Accepted.
6. **Defer** any future LLM-driven semantic analysis to **MCP Sampling**. When DocuMCP genuinely needs an LLM completion (for example, context-aware example generation that AST cannot reach), it will issue a `sampling/createMessage` request through the host so the host's LLM, policy, and quotas apply uniformly.
7. **Ship** under semver as **v0.6.0** with a `BREAKING CHANGE:` footer in [CHANGELOG.md](../../CHANGELOG.md) and a clear migration note for hybrid-mode users.

## **Refactor** `src/utils/semantic-analyzer.ts` to AST-only mode: remove `useLLM`, `confidenceThreshold`, and `llmConfig` options from `createSemanticAnalyzer()`; `analysisMode` always returns `'ast'`; the `SemanticAnalyzer` public API stays compatible where reasonable.

<!-- task-id: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling-md/refactor-src-utils-semantic-analyzer-ts-to-ast-only-mode-remove-usellm-confidenc/production -->
<!-- adr: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#106, tosin2013/documcp#107, tosin2013/documcp#108, tosin2013/documcp#109, tosin2013/documcp#110

Remove the bundled LLM client and all server-side LLM execution paths from DocuMCP. Specifically:

1. **Delete** `src/utils/llm-client.ts`, `src/utils/execution-simulator.ts`, `src/tools/simulate-execution.ts`, `tests/utils/llm-client.test.ts`, and the `docs/how-to/llm-integration.md` how-to.
2. **Refactor** `src/utils/semantic-analyzer.ts` to AST-only mode: remove `useLLM`, `confidenceThreshold`, and `llmConfig` options from `createSemanticAnalyzer()`; `analysisMode` always returns `'ast'`; the `SemanticAnalyzer` public API stays compatible where reasonable.
3. **Drop** `DOCUMCP_LLM_PROVIDER`, `DOCUMCP_LLM_API_KEY`, `DOCUMCP_LLM_MODEL`, and `DOCUMCP_LLM_BASE_URL` parsing from `src/index.ts`. Emit a one-version deprecation warning before deleting the parsing logic to give downstream users a clear signal.
4. **Remove** the `simulate_execution` tool entry from the `TOOLS` array and `CallToolRequestSchema` switch in `src/index.ts`.
5. **Mark** Phase 2.5 of ADR-009 as **Superseded** by this ADR. The rest of ADR-009 (the broader content-accuracy framework) remains Accepted.
6. **Defer** any future LLM-driven semantic analysis to **MCP Sampling**. When DocuMCP genuinely needs an LLM completion (for example, context-aware example generation that AST cannot reach), it will issue a `sampling/createMessage` request through the host so the host's LLM, policy, and quotas apply uniformly.
7. **Ship** under semver as **v0.6.0** with a `BREAKING CHANGE:` footer in [CHANGELOG.md](../../CHANGELOG.md) and a clear migration note for hybrid-mode users.

## Write tests for: **Drop** `DOCUMCP_LLM_PROVIDER`, `DOCUMCP_LLM_API_KEY`, `DOCUMCP_LLM_MODEL`, and `DOCUMCP_LLM_BASE_URL` parsing from `src/index.ts`. Emit a one-version deprecation warning before deleting the parsing logic to give downstream users a clear signal.

<!-- task-id: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling-md/drop-documcp-llm-provider-documcp-llm-api-key-documcp-llm-model-and-documcp-llm-/test -->
<!-- adr: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#106, tosin2013/documcp#107, tosin2013/documcp#108, tosin2013/documcp#109, tosin2013/documcp#110

Remove the bundled LLM client and all server-side LLM execution paths from DocuMCP. Specifically:

1. **Delete** `src/utils/llm-client.ts`, `src/utils/execution-simulator.ts`, `src/tools/simulate-execution.ts`, `tests/utils/llm-client.test.ts`, and the `docs/how-to/llm-integration.md` how-to.
2. **Refactor** `src/utils/semantic-analyzer.ts` to AST-only mode: remove `useLLM`, `confidenceThreshold`, and `llmConfig` options from `createSemanticAnalyzer()`; `analysisMode` always returns `'ast'`; the `SemanticAnalyzer` public API stays compatible where reasonable.
3. **Drop** `DOCUMCP_LLM_PROVIDER`, `DOCUMCP_LLM_API_KEY`, `DOCUMCP_LLM_MODEL`, and `DOCUMCP_LLM_BASE_URL` parsing from `src/index.ts`. Emit a one-version deprecation warning before deleting the parsing logic to give downstream users a clear signal.
4. **Remove** the `simulate_execution` tool entry from the `TOOLS` array and `CallToolRequestSchema` switch in `src/index.ts`.
5. **Mark** Phase 2.5 of ADR-009 as **Superseded** by this ADR. The rest of ADR-009 (the broader content-accuracy framework) remains Accepted.
6. **Defer** any future LLM-driven semantic analysis to **MCP Sampling**. When DocuMCP genuinely needs an LLM completion (for example, context-aware example generation that AST cannot reach), it will issue a `sampling/createMessage` request through the host so the host's LLM, policy, and quotas apply uniformly.
7. **Ship** under semver as **v0.6.0** with a `BREAKING CHANGE:` footer in [CHANGELOG.md](../../CHANGELOG.md) and a clear migration note for hybrid-mode users.

## **Drop** `DOCUMCP_LLM_PROVIDER`, `DOCUMCP_LLM_API_KEY`, `DOCUMCP_LLM_MODEL`, and `DOCUMCP_LLM_BASE_URL` parsing from `src/index.ts`. Emit a one-version deprecation warning before deleting the parsing logic to give downstream users a clear signal.

<!-- task-id: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling-md/drop-documcp-llm-provider-documcp-llm-api-key-documcp-llm-model-and-documcp-llm-/production -->
<!-- adr: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#106, tosin2013/documcp#107, tosin2013/documcp#108, tosin2013/documcp#109, tosin2013/documcp#110

Remove the bundled LLM client and all server-side LLM execution paths from DocuMCP. Specifically:

1. **Delete** `src/utils/llm-client.ts`, `src/utils/execution-simulator.ts`, `src/tools/simulate-execution.ts`, `tests/utils/llm-client.test.ts`, and the `docs/how-to/llm-integration.md` how-to.
2. **Refactor** `src/utils/semantic-analyzer.ts` to AST-only mode: remove `useLLM`, `confidenceThreshold`, and `llmConfig` options from `createSemanticAnalyzer()`; `analysisMode` always returns `'ast'`; the `SemanticAnalyzer` public API stays compatible where reasonable.
3. **Drop** `DOCUMCP_LLM_PROVIDER`, `DOCUMCP_LLM_API_KEY`, `DOCUMCP_LLM_MODEL`, and `DOCUMCP_LLM_BASE_URL` parsing from `src/index.ts`. Emit a one-version deprecation warning before deleting the parsing logic to give downstream users a clear signal.
4. **Remove** the `simulate_execution` tool entry from the `TOOLS` array and `CallToolRequestSchema` switch in `src/index.ts`.
5. **Mark** Phase 2.5 of ADR-009 as **Superseded** by this ADR. The rest of ADR-009 (the broader content-accuracy framework) remains Accepted.
6. **Defer** any future LLM-driven semantic analysis to **MCP Sampling**. When DocuMCP genuinely needs an LLM completion (for example, context-aware example generation that AST cannot reach), it will issue a `sampling/createMessage` request through the host so the host's LLM, policy, and quotas apply uniformly.
7. **Ship** under semver as **v0.6.0** with a `BREAKING CHANGE:` footer in [CHANGELOG.md](../../CHANGELOG.md) and a clear migration note for hybrid-mode users.

## Write tests for: **Remove** the `simulate_execution` tool entry from the `TOOLS` array and `CallToolRequestSchema` switch in `src/index.ts`.

<!-- task-id: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling-md/remove-the-simulate-execution-tool-entry-from-the-tools-array-and-calltoolreques/test -->
<!-- adr: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#106, tosin2013/documcp#107, tosin2013/documcp#108, tosin2013/documcp#109, tosin2013/documcp#110

Remove the bundled LLM client and all server-side LLM execution paths from DocuMCP. Specifically:

1. **Delete** `src/utils/llm-client.ts`, `src/utils/execution-simulator.ts`, `src/tools/simulate-execution.ts`, `tests/utils/llm-client.test.ts`, and the `docs/how-to/llm-integration.md` how-to.
2. **Refactor** `src/utils/semantic-analyzer.ts` to AST-only mode: remove `useLLM`, `confidenceThreshold`, and `llmConfig` options from `createSemanticAnalyzer()`; `analysisMode` always returns `'ast'`; the `SemanticAnalyzer` public API stays compatible where reasonable.
3. **Drop** `DOCUMCP_LLM_PROVIDER`, `DOCUMCP_LLM_API_KEY`, `DOCUMCP_LLM_MODEL`, and `DOCUMCP_LLM_BASE_URL` parsing from `src/index.ts`. Emit a one-version deprecation warning before deleting the parsing logic to give downstream users a clear signal.
4. **Remove** the `simulate_execution` tool entry from the `TOOLS` array and `CallToolRequestSchema` switch in `src/index.ts`.
5. **Mark** Phase 2.5 of ADR-009 as **Superseded** by this ADR. The rest of ADR-009 (the broader content-accuracy framework) remains Accepted.
6. **Defer** any future LLM-driven semantic analysis to **MCP Sampling**. When DocuMCP genuinely needs an LLM completion (for example, context-aware example generation that AST cannot reach), it will issue a `sampling/createMessage` request through the host so the host's LLM, policy, and quotas apply uniformly.
7. **Ship** under semver as **v0.6.0** with a `BREAKING CHANGE:` footer in [CHANGELOG.md](../../CHANGELOG.md) and a clear migration note for hybrid-mode users.

## **Remove** the `simulate_execution` tool entry from the `TOOLS` array and `CallToolRequestSchema` switch in `src/index.ts`.

<!-- task-id: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling-md/remove-the-simulate-execution-tool-entry-from-the-tools-array-and-calltoolreques/production -->
<!-- adr: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#106, tosin2013/documcp#107, tosin2013/documcp#108, tosin2013/documcp#109, tosin2013/documcp#110

Remove the bundled LLM client and all server-side LLM execution paths from DocuMCP. Specifically:

1. **Delete** `src/utils/llm-client.ts`, `src/utils/execution-simulator.ts`, `src/tools/simulate-execution.ts`, `tests/utils/llm-client.test.ts`, and the `docs/how-to/llm-integration.md` how-to.
2. **Refactor** `src/utils/semantic-analyzer.ts` to AST-only mode: remove `useLLM`, `confidenceThreshold`, and `llmConfig` options from `createSemanticAnalyzer()`; `analysisMode` always returns `'ast'`; the `SemanticAnalyzer` public API stays compatible where reasonable.
3. **Drop** `DOCUMCP_LLM_PROVIDER`, `DOCUMCP_LLM_API_KEY`, `DOCUMCP_LLM_MODEL`, and `DOCUMCP_LLM_BASE_URL` parsing from `src/index.ts`. Emit a one-version deprecation warning before deleting the parsing logic to give downstream users a clear signal.
4. **Remove** the `simulate_execution` tool entry from the `TOOLS` array and `CallToolRequestSchema` switch in `src/index.ts`.
5. **Mark** Phase 2.5 of ADR-009 as **Superseded** by this ADR. The rest of ADR-009 (the broader content-accuracy framework) remains Accepted.
6. **Defer** any future LLM-driven semantic analysis to **MCP Sampling**. When DocuMCP genuinely needs an LLM completion (for example, context-aware example generation that AST cannot reach), it will issue a `sampling/createMessage` request through the host so the host's LLM, policy, and quotas apply uniformly.
7. **Ship** under semver as **v0.6.0** with a `BREAKING CHANGE:` footer in [CHANGELOG.md](../../CHANGELOG.md) and a clear migration note for hybrid-mode users.

## Write tests for: **Mark** Phase 2.5 of ADR-009 as **Superseded** by this ADR. The rest of ADR-009 (the broader content-accuracy framework) remains Accepted.

<!-- task-id: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling-md/mark-phase-2-5-of-adr-009-as-superseded-by-this-adr-the-rest-of-adr-009-the-broa/test -->
<!-- adr: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#106, tosin2013/documcp#107, tosin2013/documcp#108, tosin2013/documcp#109, tosin2013/documcp#110

Remove the bundled LLM client and all server-side LLM execution paths from DocuMCP. Specifically:

1. **Delete** `src/utils/llm-client.ts`, `src/utils/execution-simulator.ts`, `src/tools/simulate-execution.ts`, `tests/utils/llm-client.test.ts`, and the `docs/how-to/llm-integration.md` how-to.
2. **Refactor** `src/utils/semantic-analyzer.ts` to AST-only mode: remove `useLLM`, `confidenceThreshold`, and `llmConfig` options from `createSemanticAnalyzer()`; `analysisMode` always returns `'ast'`; the `SemanticAnalyzer` public API stays compatible where reasonable.
3. **Drop** `DOCUMCP_LLM_PROVIDER`, `DOCUMCP_LLM_API_KEY`, `DOCUMCP_LLM_MODEL`, and `DOCUMCP_LLM_BASE_URL` parsing from `src/index.ts`. Emit a one-version deprecation warning before deleting the parsing logic to give downstream users a clear signal.
4. **Remove** the `simulate_execution` tool entry from the `TOOLS` array and `CallToolRequestSchema` switch in `src/index.ts`.
5. **Mark** Phase 2.5 of ADR-009 as **Superseded** by this ADR. The rest of ADR-009 (the broader content-accuracy framework) remains Accepted.
6. **Defer** any future LLM-driven semantic analysis to **MCP Sampling**. When DocuMCP genuinely needs an LLM completion (for example, context-aware example generation that AST cannot reach), it will issue a `sampling/createMessage` request through the host so the host's LLM, policy, and quotas apply uniformly.
7. **Ship** under semver as **v0.6.0** with a `BREAKING CHANGE:` footer in [CHANGELOG.md](../../CHANGELOG.md) and a clear migration note for hybrid-mode users.

## **Mark** Phase 2.5 of ADR-009 as **Superseded** by this ADR. The rest of ADR-009 (the broader content-accuracy framework) remains Accepted.

<!-- task-id: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling-md/mark-phase-2-5-of-adr-009-as-superseded-by-this-adr-the-rest-of-adr-009-the-broa/production -->
<!-- adr: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#106, tosin2013/documcp#107, tosin2013/documcp#108, tosin2013/documcp#109, tosin2013/documcp#110

Remove the bundled LLM client and all server-side LLM execution paths from DocuMCP. Specifically:

1. **Delete** `src/utils/llm-client.ts`, `src/utils/execution-simulator.ts`, `src/tools/simulate-execution.ts`, `tests/utils/llm-client.test.ts`, and the `docs/how-to/llm-integration.md` how-to.
2. **Refactor** `src/utils/semantic-analyzer.ts` to AST-only mode: remove `useLLM`, `confidenceThreshold`, and `llmConfig` options from `createSemanticAnalyzer()`; `analysisMode` always returns `'ast'`; the `SemanticAnalyzer` public API stays compatible where reasonable.
3. **Drop** `DOCUMCP_LLM_PROVIDER`, `DOCUMCP_LLM_API_KEY`, `DOCUMCP_LLM_MODEL`, and `DOCUMCP_LLM_BASE_URL` parsing from `src/index.ts`. Emit a one-version deprecation warning before deleting the parsing logic to give downstream users a clear signal.
4. **Remove** the `simulate_execution` tool entry from the `TOOLS` array and `CallToolRequestSchema` switch in `src/index.ts`.
5. **Mark** Phase 2.5 of ADR-009 as **Superseded** by this ADR. The rest of ADR-009 (the broader content-accuracy framework) remains Accepted.
6. **Defer** any future LLM-driven semantic analysis to **MCP Sampling**. When DocuMCP genuinely needs an LLM completion (for example, context-aware example generation that AST cannot reach), it will issue a `sampling/createMessage` request through the host so the host's LLM, policy, and quotas apply uniformly.
7. **Ship** under semver as **v0.6.0** with a `BREAKING CHANGE:` footer in [CHANGELOG.md](../../CHANGELOG.md) and a clear migration note for hybrid-mode users.

## Write tests for: **Defer** any future LLM-driven semantic analysis to **MCP Sampling**. When DocuMCP genuinely needs an LLM completion (for example, context-aware example generation that AST cannot reach), it will issue a `sampling/createMessage` request through the host so the host's LLM, policy, and quotas apply uniformly.

<!-- task-id: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling-md/defer-any-future-llm-driven-semantic-analysis-to-mcp-sampling-when-documcp-genui/test -->
<!-- adr: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#106, tosin2013/documcp#107, tosin2013/documcp#108, tosin2013/documcp#109, tosin2013/documcp#110

Remove the bundled LLM client and all server-side LLM execution paths from DocuMCP. Specifically:

1. **Delete** `src/utils/llm-client.ts`, `src/utils/execution-simulator.ts`, `src/tools/simulate-execution.ts`, `tests/utils/llm-client.test.ts`, and the `docs/how-to/llm-integration.md` how-to.
2. **Refactor** `src/utils/semantic-analyzer.ts` to AST-only mode: remove `useLLM`, `confidenceThreshold`, and `llmConfig` options from `createSemanticAnalyzer()`; `analysisMode` always returns `'ast'`; the `SemanticAnalyzer` public API stays compatible where reasonable.
3. **Drop** `DOCUMCP_LLM_PROVIDER`, `DOCUMCP_LLM_API_KEY`, `DOCUMCP_LLM_MODEL`, and `DOCUMCP_LLM_BASE_URL` parsing from `src/index.ts`. Emit a one-version deprecation warning before deleting the parsing logic to give downstream users a clear signal.
4. **Remove** the `simulate_execution` tool entry from the `TOOLS` array and `CallToolRequestSchema` switch in `src/index.ts`.
5. **Mark** Phase 2.5 of ADR-009 as **Superseded** by this ADR. The rest of ADR-009 (the broader content-accuracy framework) remains Accepted.
6. **Defer** any future LLM-driven semantic analysis to **MCP Sampling**. When DocuMCP genuinely needs an LLM completion (for example, context-aware example generation that AST cannot reach), it will issue a `sampling/createMessage` request through the host so the host's LLM, policy, and quotas apply uniformly.
7. **Ship** under semver as **v0.6.0** with a `BREAKING CHANGE:` footer in [CHANGELOG.md](../../CHANGELOG.md) and a clear migration note for hybrid-mode users.

## **Defer** any future LLM-driven semantic analysis to **MCP Sampling**. When DocuMCP genuinely needs an LLM completion (for example, context-aware example generation that AST cannot reach), it will issue a `sampling/createMessage` request through the host so the host's LLM, policy, and quotas apply uniformly.

<!-- task-id: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling-md/defer-any-future-llm-driven-semantic-analysis-to-mcp-sampling-when-documcp-genui/production -->
<!-- adr: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#106, tosin2013/documcp#107, tosin2013/documcp#108, tosin2013/documcp#109, tosin2013/documcp#110

Remove the bundled LLM client and all server-side LLM execution paths from DocuMCP. Specifically:

1. **Delete** `src/utils/llm-client.ts`, `src/utils/execution-simulator.ts`, `src/tools/simulate-execution.ts`, `tests/utils/llm-client.test.ts`, and the `docs/how-to/llm-integration.md` how-to.
2. **Refactor** `src/utils/semantic-analyzer.ts` to AST-only mode: remove `useLLM`, `confidenceThreshold`, and `llmConfig` options from `createSemanticAnalyzer()`; `analysisMode` always returns `'ast'`; the `SemanticAnalyzer` public API stays compatible where reasonable.
3. **Drop** `DOCUMCP_LLM_PROVIDER`, `DOCUMCP_LLM_API_KEY`, `DOCUMCP_LLM_MODEL`, and `DOCUMCP_LLM_BASE_URL` parsing from `src/index.ts`. Emit a one-version deprecation warning before deleting the parsing logic to give downstream users a clear signal.
4. **Remove** the `simulate_execution` tool entry from the `TOOLS` array and `CallToolRequestSchema` switch in `src/index.ts`.
5. **Mark** Phase 2.5 of ADR-009 as **Superseded** by this ADR. The rest of ADR-009 (the broader content-accuracy framework) remains Accepted.
6. **Defer** any future LLM-driven semantic analysis to **MCP Sampling**. When DocuMCP genuinely needs an LLM completion (for example, context-aware example generation that AST cannot reach), it will issue a `sampling/createMessage` request through the host so the host's LLM, policy, and quotas apply uniformly.
7. **Ship** under semver as **v0.6.0** with a `BREAKING CHANGE:` footer in [CHANGELOG.md](../../CHANGELOG.md) and a clear migration note for hybrid-mode users.

## Write tests for: **Ship** under semver as **v0.6.0** with a `BREAKING CHANGE:` footer in [CHANGELOG.md](../../CHANGELOG.md) and a clear migration note for hybrid-mode users.

<!-- task-id: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling-md/ship-under-semver-as-v0-6-0-with-a-breaking-change-footer-in-changelog-md-change/test -->
<!-- adr: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#106, tosin2013/documcp#107, tosin2013/documcp#108, tosin2013/documcp#109, tosin2013/documcp#110

Remove the bundled LLM client and all server-side LLM execution paths from DocuMCP. Specifically:

1. **Delete** `src/utils/llm-client.ts`, `src/utils/execution-simulator.ts`, `src/tools/simulate-execution.ts`, `tests/utils/llm-client.test.ts`, and the `docs/how-to/llm-integration.md` how-to.
2. **Refactor** `src/utils/semantic-analyzer.ts` to AST-only mode: remove `useLLM`, `confidenceThreshold`, and `llmConfig` options from `createSemanticAnalyzer()`; `analysisMode` always returns `'ast'`; the `SemanticAnalyzer` public API stays compatible where reasonable.
3. **Drop** `DOCUMCP_LLM_PROVIDER`, `DOCUMCP_LLM_API_KEY`, `DOCUMCP_LLM_MODEL`, and `DOCUMCP_LLM_BASE_URL` parsing from `src/index.ts`. Emit a one-version deprecation warning before deleting the parsing logic to give downstream users a clear signal.
4. **Remove** the `simulate_execution` tool entry from the `TOOLS` array and `CallToolRequestSchema` switch in `src/index.ts`.
5. **Mark** Phase 2.5 of ADR-009 as **Superseded** by this ADR. The rest of ADR-009 (the broader content-accuracy framework) remains Accepted.
6. **Defer** any future LLM-driven semantic analysis to **MCP Sampling**. When DocuMCP genuinely needs an LLM completion (for example, context-aware example generation that AST cannot reach), it will issue a `sampling/createMessage` request through the host so the host's LLM, policy, and quotas apply uniformly.
7. **Ship** under semver as **v0.6.0** with a `BREAKING CHANGE:` footer in [CHANGELOG.md](../../CHANGELOG.md) and a clear migration note for hybrid-mode users.

## **Ship** under semver as **v0.6.0** with a `BREAKING CHANGE:` footer in [CHANGELOG.md](../../CHANGELOG.md) and a clear migration note for hybrid-mode users.

<!-- task-id: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling-md/ship-under-semver-as-v0-6-0-with-a-breaking-change-footer-in-changelog-md-change/production -->
<!-- adr: adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#106, tosin2013/documcp#107, tosin2013/documcp#108, tosin2013/documcp#109, tosin2013/documcp#110

Remove the bundled LLM client and all server-side LLM execution paths from DocuMCP. Specifically:

1. **Delete** `src/utils/llm-client.ts`, `src/utils/execution-simulator.ts`, `src/tools/simulate-execution.ts`, `tests/utils/llm-client.test.ts`, and the `docs/how-to/llm-integration.md` how-to.
2. **Refactor** `src/utils/semantic-analyzer.ts` to AST-only mode: remove `useLLM`, `confidenceThreshold`, and `llmConfig` options from `createSemanticAnalyzer()`; `analysisMode` always returns `'ast'`; the `SemanticAnalyzer` public API stays compatible where reasonable.
3. **Drop** `DOCUMCP_LLM_PROVIDER`, `DOCUMCP_LLM_API_KEY`, `DOCUMCP_LLM_MODEL`, and `DOCUMCP_LLM_BASE_URL` parsing from `src/index.ts`. Emit a one-version deprecation warning before deleting the parsing logic to give downstream users a clear signal.
4. **Remove** the `simulate_execution` tool entry from the `TOOLS` array and `CallToolRequestSchema` switch in `src/index.ts`.
5. **Mark** Phase 2.5 of ADR-009 as **Superseded** by this ADR. The rest of ADR-009 (the broader content-accuracy framework) remains Accepted.
6. **Defer** any future LLM-driven semantic analysis to **MCP Sampling**. When DocuMCP genuinely needs an LLM completion (for example, context-aware example generation that AST cannot reach), it will issue a `sampling/createMessage` request through the host so the host's LLM, policy, and quotas apply uniformly.
7. **Ship** under semver as **v0.6.0** with a `BREAKING CHANGE:` footer in [CHANGELOG.md](../../CHANGELOG.md) and a clear migration note for hybrid-mode users.

## Write tests for: Define a `ParserRegistry` type in `src/utils/ast-analyzer.ts` mapping file extensions to factory functions returning a tree-sitter `Parser` instance:

<!-- task-id: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry-md/define-a-parserregistry-type-in-src-utils-ast-analyzer-ts-mapping-file-extension/test -->
<!-- adr: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#112

Adopt a **lazy-loaded parser registry pattern**.

1. Define a `ParserRegistry` type in `src/utils/ast-analyzer.ts` mapping file extensions to factory functions returning a tree-sitter `Parser` instance:

   ```ts
   const registry: ParserRegistry = {
     ".py": () => loadParser("tree-sitter-python"),
     ".go": () => loadParser("tree-sitter-go"),
     ".rs": () => loadParser("tree-sitter-rust"),
     ".java": () => loadParser("tree-sitter-java"),
     ".rb": () => loadParser("tree-sitter-ruby"),
     ".sh": () => loadParser("tree-sitter-bash"),
     ".yaml": () => loadParser("tree-sitter-yaml"),
     ".yml": () => loadParser("tree-sitter-yaml"),
   };
   ```

2. Parsers are loaded on first use and cached for the process lifetime.
3. Each parser exposes a uniform `extractSignatures(source) -> SignatureSet` API, where `SignatureSet` has the same shape as today's TS/JS output (functions, classes, exports, imports, complexity).
4. Unknown extensions fall back to a structural-only path (file size, line count, no signature extraction).
5. The drift detector in `src/utils/drift-detector.ts` consumes `SignatureSet` uniformly, regardless of source language, so adding a language is a single-file PR with no downstream changes.
6. Test fixtures live in `tests/fixtures/multi-lang/{python,go,rust,...}` so each language has a representative example exercised by the suite.
7. A performance benchmark is added to ensure parsing a 500-file polyglot repo completes in under 30 seconds in CI.

## Define a `ParserRegistry` type in `src/utils/ast-analyzer.ts` mapping file extensions to factory functions returning a tree-sitter `Parser` instance:

<!-- task-id: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry-md/define-a-parserregistry-type-in-src-utils-ast-analyzer-ts-mapping-file-extension/production -->
<!-- adr: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#112

Adopt a **lazy-loaded parser registry pattern**.

1. Define a `ParserRegistry` type in `src/utils/ast-analyzer.ts` mapping file extensions to factory functions returning a tree-sitter `Parser` instance:

   ```ts
   const registry: ParserRegistry = {
     ".py": () => loadParser("tree-sitter-python"),
     ".go": () => loadParser("tree-sitter-go"),
     ".rs": () => loadParser("tree-sitter-rust"),
     ".java": () => loadParser("tree-sitter-java"),
     ".rb": () => loadParser("tree-sitter-ruby"),
     ".sh": () => loadParser("tree-sitter-bash"),
     ".yaml": () => loadParser("tree-sitter-yaml"),
     ".yml": () => loadParser("tree-sitter-yaml"),
   };
   ```

2. Parsers are loaded on first use and cached for the process lifetime.
3. Each parser exposes a uniform `extractSignatures(source) -> SignatureSet` API, where `SignatureSet` has the same shape as today's TS/JS output (functions, classes, exports, imports, complexity).
4. Unknown extensions fall back to a structural-only path (file size, line count, no signature extraction).
5. The drift detector in `src/utils/drift-detector.ts` consumes `SignatureSet` uniformly, regardless of source language, so adding a language is a single-file PR with no downstream changes.
6. Test fixtures live in `tests/fixtures/multi-lang/{python,go,rust,...}` so each language has a representative example exercised by the suite.
7. A performance benchmark is added to ensure parsing a 500-file polyglot repo completes in under 30 seconds in CI.

## Write tests for: Parsers are loaded on first use and cached for the process lifetime.

<!-- task-id: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry-md/parsers-are-loaded-on-first-use-and-cached-for-the-process-lifetime/test -->
<!-- adr: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#112

Adopt a **lazy-loaded parser registry pattern**.

1. Define a `ParserRegistry` type in `src/utils/ast-analyzer.ts` mapping file extensions to factory functions returning a tree-sitter `Parser` instance:

   ```ts
   const registry: ParserRegistry = {
     ".py": () => loadParser("tree-sitter-python"),
     ".go": () => loadParser("tree-sitter-go"),
     ".rs": () => loadParser("tree-sitter-rust"),
     ".java": () => loadParser("tree-sitter-java"),
     ".rb": () => loadParser("tree-sitter-ruby"),
     ".sh": () => loadParser("tree-sitter-bash"),
     ".yaml": () => loadParser("tree-sitter-yaml"),
     ".yml": () => loadParser("tree-sitter-yaml"),
   };
   ```

2. Parsers are loaded on first use and cached for the process lifetime.
3. Each parser exposes a uniform `extractSignatures(source) -> SignatureSet` API, where `SignatureSet` has the same shape as today's TS/JS output (functions, classes, exports, imports, complexity).
4. Unknown extensions fall back to a structural-only path (file size, line count, no signature extraction).
5. The drift detector in `src/utils/drift-detector.ts` consumes `SignatureSet` uniformly, regardless of source language, so adding a language is a single-file PR with no downstream changes.
6. Test fixtures live in `tests/fixtures/multi-lang/{python,go,rust,...}` so each language has a representative example exercised by the suite.
7. A performance benchmark is added to ensure parsing a 500-file polyglot repo completes in under 30 seconds in CI.

## Parsers are loaded on first use and cached for the process lifetime.

<!-- task-id: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry-md/parsers-are-loaded-on-first-use-and-cached-for-the-process-lifetime/production -->
<!-- adr: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#112

Adopt a **lazy-loaded parser registry pattern**.

1. Define a `ParserRegistry` type in `src/utils/ast-analyzer.ts` mapping file extensions to factory functions returning a tree-sitter `Parser` instance:

   ```ts
   const registry: ParserRegistry = {
     ".py": () => loadParser("tree-sitter-python"),
     ".go": () => loadParser("tree-sitter-go"),
     ".rs": () => loadParser("tree-sitter-rust"),
     ".java": () => loadParser("tree-sitter-java"),
     ".rb": () => loadParser("tree-sitter-ruby"),
     ".sh": () => loadParser("tree-sitter-bash"),
     ".yaml": () => loadParser("tree-sitter-yaml"),
     ".yml": () => loadParser("tree-sitter-yaml"),
   };
   ```

2. Parsers are loaded on first use and cached for the process lifetime.
3. Each parser exposes a uniform `extractSignatures(source) -> SignatureSet` API, where `SignatureSet` has the same shape as today's TS/JS output (functions, classes, exports, imports, complexity).
4. Unknown extensions fall back to a structural-only path (file size, line count, no signature extraction).
5. The drift detector in `src/utils/drift-detector.ts` consumes `SignatureSet` uniformly, regardless of source language, so adding a language is a single-file PR with no downstream changes.
6. Test fixtures live in `tests/fixtures/multi-lang/{python,go,rust,...}` so each language has a representative example exercised by the suite.
7. A performance benchmark is added to ensure parsing a 500-file polyglot repo completes in under 30 seconds in CI.

## Write tests for: Each parser exposes a uniform `extractSignatures(source) -> SignatureSet` API, where `SignatureSet` has the same shape as today's TS/JS output (functions, classes, exports, imports, complexity).

<!-- task-id: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry-md/each-parser-exposes-a-uniform-extractsignatures-source-signatureset-api-where-si/test -->
<!-- adr: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#112

Adopt a **lazy-loaded parser registry pattern**.

1. Define a `ParserRegistry` type in `src/utils/ast-analyzer.ts` mapping file extensions to factory functions returning a tree-sitter `Parser` instance:

   ```ts
   const registry: ParserRegistry = {
     ".py": () => loadParser("tree-sitter-python"),
     ".go": () => loadParser("tree-sitter-go"),
     ".rs": () => loadParser("tree-sitter-rust"),
     ".java": () => loadParser("tree-sitter-java"),
     ".rb": () => loadParser("tree-sitter-ruby"),
     ".sh": () => loadParser("tree-sitter-bash"),
     ".yaml": () => loadParser("tree-sitter-yaml"),
     ".yml": () => loadParser("tree-sitter-yaml"),
   };
   ```

2. Parsers are loaded on first use and cached for the process lifetime.
3. Each parser exposes a uniform `extractSignatures(source) -> SignatureSet` API, where `SignatureSet` has the same shape as today's TS/JS output (functions, classes, exports, imports, complexity).
4. Unknown extensions fall back to a structural-only path (file size, line count, no signature extraction).
5. The drift detector in `src/utils/drift-detector.ts` consumes `SignatureSet` uniformly, regardless of source language, so adding a language is a single-file PR with no downstream changes.
6. Test fixtures live in `tests/fixtures/multi-lang/{python,go,rust,...}` so each language has a representative example exercised by the suite.
7. A performance benchmark is added to ensure parsing a 500-file polyglot repo completes in under 30 seconds in CI.

## Each parser exposes a uniform `extractSignatures(source) -> SignatureSet` API, where `SignatureSet` has the same shape as today's TS/JS output (functions, classes, exports, imports, complexity).

<!-- task-id: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry-md/each-parser-exposes-a-uniform-extractsignatures-source-signatureset-api-where-si/production -->
<!-- adr: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#112

Adopt a **lazy-loaded parser registry pattern**.

1. Define a `ParserRegistry` type in `src/utils/ast-analyzer.ts` mapping file extensions to factory functions returning a tree-sitter `Parser` instance:

   ```ts
   const registry: ParserRegistry = {
     ".py": () => loadParser("tree-sitter-python"),
     ".go": () => loadParser("tree-sitter-go"),
     ".rs": () => loadParser("tree-sitter-rust"),
     ".java": () => loadParser("tree-sitter-java"),
     ".rb": () => loadParser("tree-sitter-ruby"),
     ".sh": () => loadParser("tree-sitter-bash"),
     ".yaml": () => loadParser("tree-sitter-yaml"),
     ".yml": () => loadParser("tree-sitter-yaml"),
   };
   ```

2. Parsers are loaded on first use and cached for the process lifetime.
3. Each parser exposes a uniform `extractSignatures(source) -> SignatureSet` API, where `SignatureSet` has the same shape as today's TS/JS output (functions, classes, exports, imports, complexity).
4. Unknown extensions fall back to a structural-only path (file size, line count, no signature extraction).
5. The drift detector in `src/utils/drift-detector.ts` consumes `SignatureSet` uniformly, regardless of source language, so adding a language is a single-file PR with no downstream changes.
6. Test fixtures live in `tests/fixtures/multi-lang/{python,go,rust,...}` so each language has a representative example exercised by the suite.
7. A performance benchmark is added to ensure parsing a 500-file polyglot repo completes in under 30 seconds in CI.

## Write tests for: Unknown extensions fall back to a structural-only path (file size, line count, no signature extraction).

<!-- task-id: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry-md/unknown-extensions-fall-back-to-a-structural-only-path-file-size-line-count-no-s/test -->
<!-- adr: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#112

Adopt a **lazy-loaded parser registry pattern**.

1. Define a `ParserRegistry` type in `src/utils/ast-analyzer.ts` mapping file extensions to factory functions returning a tree-sitter `Parser` instance:

   ```ts
   const registry: ParserRegistry = {
     ".py": () => loadParser("tree-sitter-python"),
     ".go": () => loadParser("tree-sitter-go"),
     ".rs": () => loadParser("tree-sitter-rust"),
     ".java": () => loadParser("tree-sitter-java"),
     ".rb": () => loadParser("tree-sitter-ruby"),
     ".sh": () => loadParser("tree-sitter-bash"),
     ".yaml": () => loadParser("tree-sitter-yaml"),
     ".yml": () => loadParser("tree-sitter-yaml"),
   };
   ```

2. Parsers are loaded on first use and cached for the process lifetime.
3. Each parser exposes a uniform `extractSignatures(source) -> SignatureSet` API, where `SignatureSet` has the same shape as today's TS/JS output (functions, classes, exports, imports, complexity).
4. Unknown extensions fall back to a structural-only path (file size, line count, no signature extraction).
5. The drift detector in `src/utils/drift-detector.ts` consumes `SignatureSet` uniformly, regardless of source language, so adding a language is a single-file PR with no downstream changes.
6. Test fixtures live in `tests/fixtures/multi-lang/{python,go,rust,...}` so each language has a representative example exercised by the suite.
7. A performance benchmark is added to ensure parsing a 500-file polyglot repo completes in under 30 seconds in CI.

## Unknown extensions fall back to a structural-only path (file size, line count, no signature extraction).

<!-- task-id: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry-md/unknown-extensions-fall-back-to-a-structural-only-path-file-size-line-count-no-s/production -->
<!-- adr: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#112

Adopt a **lazy-loaded parser registry pattern**.

1. Define a `ParserRegistry` type in `src/utils/ast-analyzer.ts` mapping file extensions to factory functions returning a tree-sitter `Parser` instance:

   ```ts
   const registry: ParserRegistry = {
     ".py": () => loadParser("tree-sitter-python"),
     ".go": () => loadParser("tree-sitter-go"),
     ".rs": () => loadParser("tree-sitter-rust"),
     ".java": () => loadParser("tree-sitter-java"),
     ".rb": () => loadParser("tree-sitter-ruby"),
     ".sh": () => loadParser("tree-sitter-bash"),
     ".yaml": () => loadParser("tree-sitter-yaml"),
     ".yml": () => loadParser("tree-sitter-yaml"),
   };
   ```

2. Parsers are loaded on first use and cached for the process lifetime.
3. Each parser exposes a uniform `extractSignatures(source) -> SignatureSet` API, where `SignatureSet` has the same shape as today's TS/JS output (functions, classes, exports, imports, complexity).
4. Unknown extensions fall back to a structural-only path (file size, line count, no signature extraction).
5. The drift detector in `src/utils/drift-detector.ts` consumes `SignatureSet` uniformly, regardless of source language, so adding a language is a single-file PR with no downstream changes.
6. Test fixtures live in `tests/fixtures/multi-lang/{python,go,rust,...}` so each language has a representative example exercised by the suite.
7. A performance benchmark is added to ensure parsing a 500-file polyglot repo completes in under 30 seconds in CI.

## Write tests for: The drift detector in `src/utils/drift-detector.ts` consumes `SignatureSet` uniformly, regardless of source language, so adding a language is a single-file PR with no downstream changes.

<!-- task-id: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry-md/the-drift-detector-in-src-utils-drift-detector-ts-consumes-signatureset-uniforml/test -->
<!-- adr: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#112

Adopt a **lazy-loaded parser registry pattern**.

1. Define a `ParserRegistry` type in `src/utils/ast-analyzer.ts` mapping file extensions to factory functions returning a tree-sitter `Parser` instance:

   ```ts
   const registry: ParserRegistry = {
     ".py": () => loadParser("tree-sitter-python"),
     ".go": () => loadParser("tree-sitter-go"),
     ".rs": () => loadParser("tree-sitter-rust"),
     ".java": () => loadParser("tree-sitter-java"),
     ".rb": () => loadParser("tree-sitter-ruby"),
     ".sh": () => loadParser("tree-sitter-bash"),
     ".yaml": () => loadParser("tree-sitter-yaml"),
     ".yml": () => loadParser("tree-sitter-yaml"),
   };
   ```

2. Parsers are loaded on first use and cached for the process lifetime.
3. Each parser exposes a uniform `extractSignatures(source) -> SignatureSet` API, where `SignatureSet` has the same shape as today's TS/JS output (functions, classes, exports, imports, complexity).
4. Unknown extensions fall back to a structural-only path (file size, line count, no signature extraction).
5. The drift detector in `src/utils/drift-detector.ts` consumes `SignatureSet` uniformly, regardless of source language, so adding a language is a single-file PR with no downstream changes.
6. Test fixtures live in `tests/fixtures/multi-lang/{python,go,rust,...}` so each language has a representative example exercised by the suite.
7. A performance benchmark is added to ensure parsing a 500-file polyglot repo completes in under 30 seconds in CI.

## The drift detector in `src/utils/drift-detector.ts` consumes `SignatureSet` uniformly, regardless of source language, so adding a language is a single-file PR with no downstream changes.

<!-- task-id: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry-md/the-drift-detector-in-src-utils-drift-detector-ts-consumes-signatureset-uniforml/production -->
<!-- adr: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#112

Adopt a **lazy-loaded parser registry pattern**.

1. Define a `ParserRegistry` type in `src/utils/ast-analyzer.ts` mapping file extensions to factory functions returning a tree-sitter `Parser` instance:

   ```ts
   const registry: ParserRegistry = {
     ".py": () => loadParser("tree-sitter-python"),
     ".go": () => loadParser("tree-sitter-go"),
     ".rs": () => loadParser("tree-sitter-rust"),
     ".java": () => loadParser("tree-sitter-java"),
     ".rb": () => loadParser("tree-sitter-ruby"),
     ".sh": () => loadParser("tree-sitter-bash"),
     ".yaml": () => loadParser("tree-sitter-yaml"),
     ".yml": () => loadParser("tree-sitter-yaml"),
   };
   ```

2. Parsers are loaded on first use and cached for the process lifetime.
3. Each parser exposes a uniform `extractSignatures(source) -> SignatureSet` API, where `SignatureSet` has the same shape as today's TS/JS output (functions, classes, exports, imports, complexity).
4. Unknown extensions fall back to a structural-only path (file size, line count, no signature extraction).
5. The drift detector in `src/utils/drift-detector.ts` consumes `SignatureSet` uniformly, regardless of source language, so adding a language is a single-file PR with no downstream changes.
6. Test fixtures live in `tests/fixtures/multi-lang/{python,go,rust,...}` so each language has a representative example exercised by the suite.
7. A performance benchmark is added to ensure parsing a 500-file polyglot repo completes in under 30 seconds in CI.

## Write tests for: Test fixtures live in `tests/fixtures/multi-lang/{python,go,rust,...}` so each language has a representative example exercised by the suite.

<!-- task-id: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry-md/test-fixtures-live-in-tests-fixtures-multi-lang-python-go-rust-so-each-language-/test -->
<!-- adr: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#112

Adopt a **lazy-loaded parser registry pattern**.

1. Define a `ParserRegistry` type in `src/utils/ast-analyzer.ts` mapping file extensions to factory functions returning a tree-sitter `Parser` instance:

   ```ts
   const registry: ParserRegistry = {
     ".py": () => loadParser("tree-sitter-python"),
     ".go": () => loadParser("tree-sitter-go"),
     ".rs": () => loadParser("tree-sitter-rust"),
     ".java": () => loadParser("tree-sitter-java"),
     ".rb": () => loadParser("tree-sitter-ruby"),
     ".sh": () => loadParser("tree-sitter-bash"),
     ".yaml": () => loadParser("tree-sitter-yaml"),
     ".yml": () => loadParser("tree-sitter-yaml"),
   };
   ```

2. Parsers are loaded on first use and cached for the process lifetime.
3. Each parser exposes a uniform `extractSignatures(source) -> SignatureSet` API, where `SignatureSet` has the same shape as today's TS/JS output (functions, classes, exports, imports, complexity).
4. Unknown extensions fall back to a structural-only path (file size, line count, no signature extraction).
5. The drift detector in `src/utils/drift-detector.ts` consumes `SignatureSet` uniformly, regardless of source language, so adding a language is a single-file PR with no downstream changes.
6. Test fixtures live in `tests/fixtures/multi-lang/{python,go,rust,...}` so each language has a representative example exercised by the suite.
7. A performance benchmark is added to ensure parsing a 500-file polyglot repo completes in under 30 seconds in CI.

## Test fixtures live in `tests/fixtures/multi-lang/{python,go,rust,...}` so each language has a representative example exercised by the suite.

<!-- task-id: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry-md/test-fixtures-live-in-tests-fixtures-multi-lang-python-go-rust-so-each-language-/production -->
<!-- adr: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#112

Adopt a **lazy-loaded parser registry pattern**.

1. Define a `ParserRegistry` type in `src/utils/ast-analyzer.ts` mapping file extensions to factory functions returning a tree-sitter `Parser` instance:

   ```ts
   const registry: ParserRegistry = {
     ".py": () => loadParser("tree-sitter-python"),
     ".go": () => loadParser("tree-sitter-go"),
     ".rs": () => loadParser("tree-sitter-rust"),
     ".java": () => loadParser("tree-sitter-java"),
     ".rb": () => loadParser("tree-sitter-ruby"),
     ".sh": () => loadParser("tree-sitter-bash"),
     ".yaml": () => loadParser("tree-sitter-yaml"),
     ".yml": () => loadParser("tree-sitter-yaml"),
   };
   ```

2. Parsers are loaded on first use and cached for the process lifetime.
3. Each parser exposes a uniform `extractSignatures(source) -> SignatureSet` API, where `SignatureSet` has the same shape as today's TS/JS output (functions, classes, exports, imports, complexity).
4. Unknown extensions fall back to a structural-only path (file size, line count, no signature extraction).
5. The drift detector in `src/utils/drift-detector.ts` consumes `SignatureSet` uniformly, regardless of source language, so adding a language is a single-file PR with no downstream changes.
6. Test fixtures live in `tests/fixtures/multi-lang/{python,go,rust,...}` so each language has a representative example exercised by the suite.
7. A performance benchmark is added to ensure parsing a 500-file polyglot repo completes in under 30 seconds in CI.

## Write tests for: A performance benchmark is added to ensure parsing a 500-file polyglot repo completes in under 30 seconds in CI.

<!-- task-id: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry-md/a-performance-benchmark-is-added-to-ensure-parsing-a-500-file-polyglot-repo-comp/test -->
<!-- adr: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#112

Adopt a **lazy-loaded parser registry pattern**.

1. Define a `ParserRegistry` type in `src/utils/ast-analyzer.ts` mapping file extensions to factory functions returning a tree-sitter `Parser` instance:

   ```ts
   const registry: ParserRegistry = {
     ".py": () => loadParser("tree-sitter-python"),
     ".go": () => loadParser("tree-sitter-go"),
     ".rs": () => loadParser("tree-sitter-rust"),
     ".java": () => loadParser("tree-sitter-java"),
     ".rb": () => loadParser("tree-sitter-ruby"),
     ".sh": () => loadParser("tree-sitter-bash"),
     ".yaml": () => loadParser("tree-sitter-yaml"),
     ".yml": () => loadParser("tree-sitter-yaml"),
   };
   ```

2. Parsers are loaded on first use and cached for the process lifetime.
3. Each parser exposes a uniform `extractSignatures(source) -> SignatureSet` API, where `SignatureSet` has the same shape as today's TS/JS output (functions, classes, exports, imports, complexity).
4. Unknown extensions fall back to a structural-only path (file size, line count, no signature extraction).
5. The drift detector in `src/utils/drift-detector.ts` consumes `SignatureSet` uniformly, regardless of source language, so adding a language is a single-file PR with no downstream changes.
6. Test fixtures live in `tests/fixtures/multi-lang/{python,go,rust,...}` so each language has a representative example exercised by the suite.
7. A performance benchmark is added to ensure parsing a 500-file polyglot repo completes in under 30 seconds in CI.

## A performance benchmark is added to ensure parsing a 500-file polyglot repo completes in under 30 seconds in CI.

<!-- task-id: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry-md/a-performance-benchmark-is-added-to-ensure-parsing-a-500-file-polyglot-repo-comp/production -->
<!-- adr: adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#112

Adopt a **lazy-loaded parser registry pattern**.

1. Define a `ParserRegistry` type in `src/utils/ast-analyzer.ts` mapping file extensions to factory functions returning a tree-sitter `Parser` instance:

   ```ts
   const registry: ParserRegistry = {
     ".py": () => loadParser("tree-sitter-python"),
     ".go": () => loadParser("tree-sitter-go"),
     ".rs": () => loadParser("tree-sitter-rust"),
     ".java": () => loadParser("tree-sitter-java"),
     ".rb": () => loadParser("tree-sitter-ruby"),
     ".sh": () => loadParser("tree-sitter-bash"),
     ".yaml": () => loadParser("tree-sitter-yaml"),
     ".yml": () => loadParser("tree-sitter-yaml"),
   };
   ```

2. Parsers are loaded on first use and cached for the process lifetime.
3. Each parser exposes a uniform `extractSignatures(source) -> SignatureSet` API, where `SignatureSet` has the same shape as today's TS/JS output (functions, classes, exports, imports, complexity).
4. Unknown extensions fall back to a structural-only path (file size, line count, no signature extraction).
5. The drift detector in `src/utils/drift-detector.ts` consumes `SignatureSet` uniformly, regardless of source language, so adding a language is a single-file PR with no downstream changes.
6. Test fixtures live in `tests/fixtures/multi-lang/{python,go,rust,...}` so each language has a representative example exercised by the suite.
7. A performance benchmark is added to ensure parsing a 500-file polyglot repo completes in under 30 seconds in CI.

## Write tests for: **`metadata: AdapterMetadata`** — human-readable name, slug, supported features, and signal weights for detection.

<!-- task-id: adr-0016-ssg-adapter-plugin-pattern-for-recommendation-configuration-and-deploym/metadata-adaptermetadata-human-readable-name-slug-supported-features-and-signal-/test -->
<!-- adr: adr-0016-ssg-adapter-plugin-pattern-for-recommendation-configuration-and-deployment.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#122, tosin2013/documcp#123

Define an `SSGAdapter` interface in `src/adapters/types.ts` with four members:

```ts
export interface SSGAdapter {
  metadata: AdapterMetadata;
  detect(analysis: RepositoryAnalysis): DetectionScore;
  generateConfig(opts: ConfigOptions): GeneratedFile[];
  generateWorkflow(opts: WorkflowOptions): string;
}
```

1. **`metadata: AdapterMetadata`** — human-readable name, slug, supported features, and signal weights for detection.
2. **`detect(analysis) -> DetectionScore`** — returns a 0-1 confidence that this SSG fits the repo, replacing the current scoring branches in `recommend-ssg`.
3. **`generateConfig(opts) -> GeneratedFile[]`** — emits one or more files (e.g., `docusaurus.config.ts`, `astro.config.mjs`) ready to write to disk.
4. **`generateWorkflow(opts) -> string`** — emits a GitHub Actions YAML workflow specific to the SSG.

Concrete adapters live in `src/adapters/<ssg>/index.ts` (e.g., `src/adapters/docusaurus/index.ts`). The three tools iterate the adapter registry instead of switching on a hard-coded enum. The five existing SSGs are migrated onto the interface in a single refactor PR; new SSGs (Astro, VitePress) ship as the first net-new additions through this contract.

## **`metadata: AdapterMetadata`** — human-readable name, slug, supported features, and signal weights for detection.

<!-- task-id: adr-0016-ssg-adapter-plugin-pattern-for-recommendation-configuration-and-deploym/metadata-adaptermetadata-human-readable-name-slug-supported-features-and-signal-/production -->
<!-- adr: adr-0016-ssg-adapter-plugin-pattern-for-recommendation-configuration-and-deployment.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#122, tosin2013/documcp#123

Define an `SSGAdapter` interface in `src/adapters/types.ts` with four members:

```ts
export interface SSGAdapter {
  metadata: AdapterMetadata;
  detect(analysis: RepositoryAnalysis): DetectionScore;
  generateConfig(opts: ConfigOptions): GeneratedFile[];
  generateWorkflow(opts: WorkflowOptions): string;
}
```

1. **`metadata: AdapterMetadata`** — human-readable name, slug, supported features, and signal weights for detection.
2. **`detect(analysis) -> DetectionScore`** — returns a 0-1 confidence that this SSG fits the repo, replacing the current scoring branches in `recommend-ssg`.
3. **`generateConfig(opts) -> GeneratedFile[]`** — emits one or more files (e.g., `docusaurus.config.ts`, `astro.config.mjs`) ready to write to disk.
4. **`generateWorkflow(opts) -> string`** — emits a GitHub Actions YAML workflow specific to the SSG.

Concrete adapters live in `src/adapters/<ssg>/index.ts` (e.g., `src/adapters/docusaurus/index.ts`). The three tools iterate the adapter registry instead of switching on a hard-coded enum. The five existing SSGs are migrated onto the interface in a single refactor PR; new SSGs (Astro, VitePress) ship as the first net-new additions through this contract.

## Write tests for: **`detect(analysis) -> DetectionScore`** — returns a 0-1 confidence that this SSG fits the repo, replacing the current scoring branches in `recommend-ssg`.

<!-- task-id: adr-0016-ssg-adapter-plugin-pattern-for-recommendation-configuration-and-deploym/detect-analysis-detectionscore-returns-a-0-1-confidence-that-this-ssg-fits-the-r/test -->
<!-- adr: adr-0016-ssg-adapter-plugin-pattern-for-recommendation-configuration-and-deployment.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#122, tosin2013/documcp#123

Define an `SSGAdapter` interface in `src/adapters/types.ts` with four members:

```ts
export interface SSGAdapter {
  metadata: AdapterMetadata;
  detect(analysis: RepositoryAnalysis): DetectionScore;
  generateConfig(opts: ConfigOptions): GeneratedFile[];
  generateWorkflow(opts: WorkflowOptions): string;
}
```

1. **`metadata: AdapterMetadata`** — human-readable name, slug, supported features, and signal weights for detection.
2. **`detect(analysis) -> DetectionScore`** — returns a 0-1 confidence that this SSG fits the repo, replacing the current scoring branches in `recommend-ssg`.
3. **`generateConfig(opts) -> GeneratedFile[]`** — emits one or more files (e.g., `docusaurus.config.ts`, `astro.config.mjs`) ready to write to disk.
4. **`generateWorkflow(opts) -> string`** — emits a GitHub Actions YAML workflow specific to the SSG.

Concrete adapters live in `src/adapters/<ssg>/index.ts` (e.g., `src/adapters/docusaurus/index.ts`). The three tools iterate the adapter registry instead of switching on a hard-coded enum. The five existing SSGs are migrated onto the interface in a single refactor PR; new SSGs (Astro, VitePress) ship as the first net-new additions through this contract.

## **`detect(analysis) -> DetectionScore`** — returns a 0-1 confidence that this SSG fits the repo, replacing the current scoring branches in `recommend-ssg`.

<!-- task-id: adr-0016-ssg-adapter-plugin-pattern-for-recommendation-configuration-and-deploym/detect-analysis-detectionscore-returns-a-0-1-confidence-that-this-ssg-fits-the-r/production -->
<!-- adr: adr-0016-ssg-adapter-plugin-pattern-for-recommendation-configuration-and-deployment.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#122, tosin2013/documcp#123

Define an `SSGAdapter` interface in `src/adapters/types.ts` with four members:

```ts
export interface SSGAdapter {
  metadata: AdapterMetadata;
  detect(analysis: RepositoryAnalysis): DetectionScore;
  generateConfig(opts: ConfigOptions): GeneratedFile[];
  generateWorkflow(opts: WorkflowOptions): string;
}
```

1. **`metadata: AdapterMetadata`** — human-readable name, slug, supported features, and signal weights for detection.
2. **`detect(analysis) -> DetectionScore`** — returns a 0-1 confidence that this SSG fits the repo, replacing the current scoring branches in `recommend-ssg`.
3. **`generateConfig(opts) -> GeneratedFile[]`** — emits one or more files (e.g., `docusaurus.config.ts`, `astro.config.mjs`) ready to write to disk.
4. **`generateWorkflow(opts) -> string`** — emits a GitHub Actions YAML workflow specific to the SSG.

Concrete adapters live in `src/adapters/<ssg>/index.ts` (e.g., `src/adapters/docusaurus/index.ts`). The three tools iterate the adapter registry instead of switching on a hard-coded enum. The five existing SSGs are migrated onto the interface in a single refactor PR; new SSGs (Astro, VitePress) ship as the first net-new additions through this contract.

## Write tests for: **`generateConfig(opts) -> GeneratedFile[]`** — emits one or more files (e.g., `docusaurus.config.ts`, `astro.config.mjs`) ready to write to disk.

<!-- task-id: adr-0016-ssg-adapter-plugin-pattern-for-recommendation-configuration-and-deploym/generateconfig-opts-generatedfile-emits-one-or-more-files-e-g-docusaurus-config-/test -->
<!-- adr: adr-0016-ssg-adapter-plugin-pattern-for-recommendation-configuration-and-deployment.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#122, tosin2013/documcp#123

Define an `SSGAdapter` interface in `src/adapters/types.ts` with four members:

```ts
export interface SSGAdapter {
  metadata: AdapterMetadata;
  detect(analysis: RepositoryAnalysis): DetectionScore;
  generateConfig(opts: ConfigOptions): GeneratedFile[];
  generateWorkflow(opts: WorkflowOptions): string;
}
```

1. **`metadata: AdapterMetadata`** — human-readable name, slug, supported features, and signal weights for detection.
2. **`detect(analysis) -> DetectionScore`** — returns a 0-1 confidence that this SSG fits the repo, replacing the current scoring branches in `recommend-ssg`.
3. **`generateConfig(opts) -> GeneratedFile[]`** — emits one or more files (e.g., `docusaurus.config.ts`, `astro.config.mjs`) ready to write to disk.
4. **`generateWorkflow(opts) -> string`** — emits a GitHub Actions YAML workflow specific to the SSG.

Concrete adapters live in `src/adapters/<ssg>/index.ts` (e.g., `src/adapters/docusaurus/index.ts`). The three tools iterate the adapter registry instead of switching on a hard-coded enum. The five existing SSGs are migrated onto the interface in a single refactor PR; new SSGs (Astro, VitePress) ship as the first net-new additions through this contract.

## **`generateConfig(opts) -> GeneratedFile[]`** — emits one or more files (e.g., `docusaurus.config.ts`, `astro.config.mjs`) ready to write to disk.

<!-- task-id: adr-0016-ssg-adapter-plugin-pattern-for-recommendation-configuration-and-deploym/generateconfig-opts-generatedfile-emits-one-or-more-files-e-g-docusaurus-config-/production -->
<!-- adr: adr-0016-ssg-adapter-plugin-pattern-for-recommendation-configuration-and-deployment.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#122, tosin2013/documcp#123

Define an `SSGAdapter` interface in `src/adapters/types.ts` with four members:

```ts
export interface SSGAdapter {
  metadata: AdapterMetadata;
  detect(analysis: RepositoryAnalysis): DetectionScore;
  generateConfig(opts: ConfigOptions): GeneratedFile[];
  generateWorkflow(opts: WorkflowOptions): string;
}
```

1. **`metadata: AdapterMetadata`** — human-readable name, slug, supported features, and signal weights for detection.
2. **`detect(analysis) -> DetectionScore`** — returns a 0-1 confidence that this SSG fits the repo, replacing the current scoring branches in `recommend-ssg`.
3. **`generateConfig(opts) -> GeneratedFile[]`** — emits one or more files (e.g., `docusaurus.config.ts`, `astro.config.mjs`) ready to write to disk.
4. **`generateWorkflow(opts) -> string`** — emits a GitHub Actions YAML workflow specific to the SSG.

Concrete adapters live in `src/adapters/<ssg>/index.ts` (e.g., `src/adapters/docusaurus/index.ts`). The three tools iterate the adapter registry instead of switching on a hard-coded enum. The five existing SSGs are migrated onto the interface in a single refactor PR; new SSGs (Astro, VitePress) ship as the first net-new additions through this contract.

## Write tests for: **`generateWorkflow(opts) -> string`** — emits a GitHub Actions YAML workflow specific to the SSG.

<!-- task-id: adr-0016-ssg-adapter-plugin-pattern-for-recommendation-configuration-and-deploym/generateworkflow-opts-string-emits-a-github-actions-yaml-workflow-specific-to-th/test -->
<!-- adr: adr-0016-ssg-adapter-plugin-pattern-for-recommendation-configuration-and-deployment.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#122, tosin2013/documcp#123

Define an `SSGAdapter` interface in `src/adapters/types.ts` with four members:

```ts
export interface SSGAdapter {
  metadata: AdapterMetadata;
  detect(analysis: RepositoryAnalysis): DetectionScore;
  generateConfig(opts: ConfigOptions): GeneratedFile[];
  generateWorkflow(opts: WorkflowOptions): string;
}
```

1. **`metadata: AdapterMetadata`** — human-readable name, slug, supported features, and signal weights for detection.
2. **`detect(analysis) -> DetectionScore`** — returns a 0-1 confidence that this SSG fits the repo, replacing the current scoring branches in `recommend-ssg`.
3. **`generateConfig(opts) -> GeneratedFile[]`** — emits one or more files (e.g., `docusaurus.config.ts`, `astro.config.mjs`) ready to write to disk.
4. **`generateWorkflow(opts) -> string`** — emits a GitHub Actions YAML workflow specific to the SSG.

Concrete adapters live in `src/adapters/<ssg>/index.ts` (e.g., `src/adapters/docusaurus/index.ts`). The three tools iterate the adapter registry instead of switching on a hard-coded enum. The five existing SSGs are migrated onto the interface in a single refactor PR; new SSGs (Astro, VitePress) ship as the first net-new additions through this contract.

## **`generateWorkflow(opts) -> string`** — emits a GitHub Actions YAML workflow specific to the SSG.

<!-- task-id: adr-0016-ssg-adapter-plugin-pattern-for-recommendation-configuration-and-deploym/generateworkflow-opts-string-emits-a-github-actions-yaml-workflow-specific-to-th/production -->
<!-- adr: adr-0016-ssg-adapter-plugin-pattern-for-recommendation-configuration-and-deployment.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#122, tosin2013/documcp#123

Define an `SSGAdapter` interface in `src/adapters/types.ts` with four members:

```ts
export interface SSGAdapter {
  metadata: AdapterMetadata;
  detect(analysis: RepositoryAnalysis): DetectionScore;
  generateConfig(opts: ConfigOptions): GeneratedFile[];
  generateWorkflow(opts: WorkflowOptions): string;
}
```

1. **`metadata: AdapterMetadata`** — human-readable name, slug, supported features, and signal weights for detection.
2. **`detect(analysis) -> DetectionScore`** — returns a 0-1 confidence that this SSG fits the repo, replacing the current scoring branches in `recommend-ssg`.
3. **`generateConfig(opts) -> GeneratedFile[]`** — emits one or more files (e.g., `docusaurus.config.ts`, `astro.config.mjs`) ready to write to disk.
4. **`generateWorkflow(opts) -> string`** — emits a GitHub Actions YAML workflow specific to the SSG.

Concrete adapters live in `src/adapters/<ssg>/index.ts` (e.g., `src/adapters/docusaurus/index.ts`). The three tools iterate the adapter registry instead of switching on a hard-coded enum. The five existing SSGs are migrated onto the interface in a single refactor PR; new SSGs (Astro, VitePress) ship as the first net-new additions through this contract.

## Write tests for: Expose this as a second `bin` entry in `package.json` (`documcp-cli`), keeping `documcp` as the MCP server entry point.

<!-- task-id: adr-0017-standalone-cli-surface-for-non-mcp-workflows-md/expose-this-as-a-second-bin-entry-in-package-json-documcp-cli-keeping-documcp-as/test -->
<!-- adr: adr-0017-standalone-cli-surface-for-non-mcp-workflows.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#126

Add `src/cli.ts` as a thin dispatcher that uses an internal `runTool(name, input)` adapter to call existing tool handlers directly, bypassing MCP request/response framing.

1. Expose this as a second `bin` entry in `package.json` (`documcp-cli`), keeping `documcp` as the MCP server entry point.
2. Subcommands mirror tool names:

   ```bash
   documcp-cli analyze <path>
   documcp-cli recommend <path>
   documcp-cli setup <path> --ssg <name>
   documcp-cli deploy <path>
   documcp-cli validate <path>
   ```

3. Each subcommand accepts the same parameters as its corresponding MCP tool, validated by the same Zod schemas.
4. Output defaults to human-readable text on stdout; `--json` toggles structured JSON for scripting.
5. Exit codes follow Unix conventions: `0` success, `1` user error, `2` system error.
6. The CLI does **not** duplicate any business logic. It is purely a different transport for the existing tool handlers, enforced by a single shared call site.

## Expose this as a second `bin` entry in `package.json` (`documcp-cli`), keeping `documcp` as the MCP server entry point.

<!-- task-id: adr-0017-standalone-cli-surface-for-non-mcp-workflows-md/expose-this-as-a-second-bin-entry-in-package-json-documcp-cli-keeping-documcp-as/production -->
<!-- adr: adr-0017-standalone-cli-surface-for-non-mcp-workflows.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#126

Add `src/cli.ts` as a thin dispatcher that uses an internal `runTool(name, input)` adapter to call existing tool handlers directly, bypassing MCP request/response framing.

1. Expose this as a second `bin` entry in `package.json` (`documcp-cli`), keeping `documcp` as the MCP server entry point.
2. Subcommands mirror tool names:

   ```bash
   documcp-cli analyze <path>
   documcp-cli recommend <path>
   documcp-cli setup <path> --ssg <name>
   documcp-cli deploy <path>
   documcp-cli validate <path>
   ```

3. Each subcommand accepts the same parameters as its corresponding MCP tool, validated by the same Zod schemas.
4. Output defaults to human-readable text on stdout; `--json` toggles structured JSON for scripting.
5. Exit codes follow Unix conventions: `0` success, `1` user error, `2` system error.
6. The CLI does **not** duplicate any business logic. It is purely a different transport for the existing tool handlers, enforced by a single shared call site.

## Write tests for: Subcommands mirror tool names:

<!-- task-id: adr-0017-standalone-cli-surface-for-non-mcp-workflows-md/subcommands-mirror-tool-names/test -->
<!-- adr: adr-0017-standalone-cli-surface-for-non-mcp-workflows.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#126

Add `src/cli.ts` as a thin dispatcher that uses an internal `runTool(name, input)` adapter to call existing tool handlers directly, bypassing MCP request/response framing.

1. Expose this as a second `bin` entry in `package.json` (`documcp-cli`), keeping `documcp` as the MCP server entry point.
2. Subcommands mirror tool names:

   ```bash
   documcp-cli analyze <path>
   documcp-cli recommend <path>
   documcp-cli setup <path> --ssg <name>
   documcp-cli deploy <path>
   documcp-cli validate <path>
   ```

3. Each subcommand accepts the same parameters as its corresponding MCP tool, validated by the same Zod schemas.
4. Output defaults to human-readable text on stdout; `--json` toggles structured JSON for scripting.
5. Exit codes follow Unix conventions: `0` success, `1` user error, `2` system error.
6. The CLI does **not** duplicate any business logic. It is purely a different transport for the existing tool handlers, enforced by a single shared call site.

## Subcommands mirror tool names:

<!-- task-id: adr-0017-standalone-cli-surface-for-non-mcp-workflows-md/subcommands-mirror-tool-names/production -->
<!-- adr: adr-0017-standalone-cli-surface-for-non-mcp-workflows.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#126

Add `src/cli.ts` as a thin dispatcher that uses an internal `runTool(name, input)` adapter to call existing tool handlers directly, bypassing MCP request/response framing.

1. Expose this as a second `bin` entry in `package.json` (`documcp-cli`), keeping `documcp` as the MCP server entry point.
2. Subcommands mirror tool names:

   ```bash
   documcp-cli analyze <path>
   documcp-cli recommend <path>
   documcp-cli setup <path> --ssg <name>
   documcp-cli deploy <path>
   documcp-cli validate <path>
   ```

3. Each subcommand accepts the same parameters as its corresponding MCP tool, validated by the same Zod schemas.
4. Output defaults to human-readable text on stdout; `--json` toggles structured JSON for scripting.
5. Exit codes follow Unix conventions: `0` success, `1` user error, `2` system error.
6. The CLI does **not** duplicate any business logic. It is purely a different transport for the existing tool handlers, enforced by a single shared call site.

## Write tests for: Each subcommand accepts the same parameters as its corresponding MCP tool, validated by the same Zod schemas.

<!-- task-id: adr-0017-standalone-cli-surface-for-non-mcp-workflows-md/each-subcommand-accepts-the-same-parameters-as-its-corresponding-mcp-tool-valida/test -->
<!-- adr: adr-0017-standalone-cli-surface-for-non-mcp-workflows.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#126

Add `src/cli.ts` as a thin dispatcher that uses an internal `runTool(name, input)` adapter to call existing tool handlers directly, bypassing MCP request/response framing.

1. Expose this as a second `bin` entry in `package.json` (`documcp-cli`), keeping `documcp` as the MCP server entry point.
2. Subcommands mirror tool names:

   ```bash
   documcp-cli analyze <path>
   documcp-cli recommend <path>
   documcp-cli setup <path> --ssg <name>
   documcp-cli deploy <path>
   documcp-cli validate <path>
   ```

3. Each subcommand accepts the same parameters as its corresponding MCP tool, validated by the same Zod schemas.
4. Output defaults to human-readable text on stdout; `--json` toggles structured JSON for scripting.
5. Exit codes follow Unix conventions: `0` success, `1` user error, `2` system error.
6. The CLI does **not** duplicate any business logic. It is purely a different transport for the existing tool handlers, enforced by a single shared call site.

## Each subcommand accepts the same parameters as its corresponding MCP tool, validated by the same Zod schemas.

<!-- task-id: adr-0017-standalone-cli-surface-for-non-mcp-workflows-md/each-subcommand-accepts-the-same-parameters-as-its-corresponding-mcp-tool-valida/production -->
<!-- adr: adr-0017-standalone-cli-surface-for-non-mcp-workflows.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#126

Add `src/cli.ts` as a thin dispatcher that uses an internal `runTool(name, input)` adapter to call existing tool handlers directly, bypassing MCP request/response framing.

1. Expose this as a second `bin` entry in `package.json` (`documcp-cli`), keeping `documcp` as the MCP server entry point.
2. Subcommands mirror tool names:

   ```bash
   documcp-cli analyze <path>
   documcp-cli recommend <path>
   documcp-cli setup <path> --ssg <name>
   documcp-cli deploy <path>
   documcp-cli validate <path>
   ```

3. Each subcommand accepts the same parameters as its corresponding MCP tool, validated by the same Zod schemas.
4. Output defaults to human-readable text on stdout; `--json` toggles structured JSON for scripting.
5. Exit codes follow Unix conventions: `0` success, `1` user error, `2` system error.
6. The CLI does **not** duplicate any business logic. It is purely a different transport for the existing tool handlers, enforced by a single shared call site.

## Write tests for: Output defaults to human-readable text on stdout; `--json` toggles structured JSON for scripting.

<!-- task-id: adr-0017-standalone-cli-surface-for-non-mcp-workflows-md/output-defaults-to-human-readable-text-on-stdout-json-toggles-structured-json-fo/test -->
<!-- adr: adr-0017-standalone-cli-surface-for-non-mcp-workflows.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#126

Add `src/cli.ts` as a thin dispatcher that uses an internal `runTool(name, input)` adapter to call existing tool handlers directly, bypassing MCP request/response framing.

1. Expose this as a second `bin` entry in `package.json` (`documcp-cli`), keeping `documcp` as the MCP server entry point.
2. Subcommands mirror tool names:

   ```bash
   documcp-cli analyze <path>
   documcp-cli recommend <path>
   documcp-cli setup <path> --ssg <name>
   documcp-cli deploy <path>
   documcp-cli validate <path>
   ```

3. Each subcommand accepts the same parameters as its corresponding MCP tool, validated by the same Zod schemas.
4. Output defaults to human-readable text on stdout; `--json` toggles structured JSON for scripting.
5. Exit codes follow Unix conventions: `0` success, `1` user error, `2` system error.
6. The CLI does **not** duplicate any business logic. It is purely a different transport for the existing tool handlers, enforced by a single shared call site.

## Output defaults to human-readable text on stdout; `--json` toggles structured JSON for scripting.

<!-- task-id: adr-0017-standalone-cli-surface-for-non-mcp-workflows-md/output-defaults-to-human-readable-text-on-stdout-json-toggles-structured-json-fo/production -->
<!-- adr: adr-0017-standalone-cli-surface-for-non-mcp-workflows.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#126

Add `src/cli.ts` as a thin dispatcher that uses an internal `runTool(name, input)` adapter to call existing tool handlers directly, bypassing MCP request/response framing.

1. Expose this as a second `bin` entry in `package.json` (`documcp-cli`), keeping `documcp` as the MCP server entry point.
2. Subcommands mirror tool names:

   ```bash
   documcp-cli analyze <path>
   documcp-cli recommend <path>
   documcp-cli setup <path> --ssg <name>
   documcp-cli deploy <path>
   documcp-cli validate <path>
   ```

3. Each subcommand accepts the same parameters as its corresponding MCP tool, validated by the same Zod schemas.
4. Output defaults to human-readable text on stdout; `--json` toggles structured JSON for scripting.
5. Exit codes follow Unix conventions: `0` success, `1` user error, `2` system error.
6. The CLI does **not** duplicate any business logic. It is purely a different transport for the existing tool handlers, enforced by a single shared call site.

## Write tests for: Exit codes follow Unix conventions: `0` success, `1` user error, `2` system error.

<!-- task-id: adr-0017-standalone-cli-surface-for-non-mcp-workflows-md/exit-codes-follow-unix-conventions-0-success-1-user-error-2-system-error/test -->
<!-- adr: adr-0017-standalone-cli-surface-for-non-mcp-workflows.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#126

Add `src/cli.ts` as a thin dispatcher that uses an internal `runTool(name, input)` adapter to call existing tool handlers directly, bypassing MCP request/response framing.

1. Expose this as a second `bin` entry in `package.json` (`documcp-cli`), keeping `documcp` as the MCP server entry point.
2. Subcommands mirror tool names:

   ```bash
   documcp-cli analyze <path>
   documcp-cli recommend <path>
   documcp-cli setup <path> --ssg <name>
   documcp-cli deploy <path>
   documcp-cli validate <path>
   ```

3. Each subcommand accepts the same parameters as its corresponding MCP tool, validated by the same Zod schemas.
4. Output defaults to human-readable text on stdout; `--json` toggles structured JSON for scripting.
5. Exit codes follow Unix conventions: `0` success, `1` user error, `2` system error.
6. The CLI does **not** duplicate any business logic. It is purely a different transport for the existing tool handlers, enforced by a single shared call site.

## Exit codes follow Unix conventions: `0` success, `1` user error, `2` system error.

<!-- task-id: adr-0017-standalone-cli-surface-for-non-mcp-workflows-md/exit-codes-follow-unix-conventions-0-success-1-user-error-2-system-error/production -->
<!-- adr: adr-0017-standalone-cli-surface-for-non-mcp-workflows.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#126

Add `src/cli.ts` as a thin dispatcher that uses an internal `runTool(name, input)` adapter to call existing tool handlers directly, bypassing MCP request/response framing.

1. Expose this as a second `bin` entry in `package.json` (`documcp-cli`), keeping `documcp` as the MCP server entry point.
2. Subcommands mirror tool names:

   ```bash
   documcp-cli analyze <path>
   documcp-cli recommend <path>
   documcp-cli setup <path> --ssg <name>
   documcp-cli deploy <path>
   documcp-cli validate <path>
   ```

3. Each subcommand accepts the same parameters as its corresponding MCP tool, validated by the same Zod schemas.
4. Output defaults to human-readable text on stdout; `--json` toggles structured JSON for scripting.
5. Exit codes follow Unix conventions: `0` success, `1` user error, `2` system error.
6. The CLI does **not** duplicate any business logic. It is purely a different transport for the existing tool handlers, enforced by a single shared call site.

## Write tests for: The CLI does **not** duplicate any business logic. It is purely a different transport for the existing tool handlers, enforced by a single shared call site.

<!-- task-id: adr-0017-standalone-cli-surface-for-non-mcp-workflows-md/the-cli-does-not-duplicate-any-business-logic-it-is-purely-a-different-transport/test -->
<!-- adr: adr-0017-standalone-cli-surface-for-non-mcp-workflows.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#126

Add `src/cli.ts` as a thin dispatcher that uses an internal `runTool(name, input)` adapter to call existing tool handlers directly, bypassing MCP request/response framing.

1. Expose this as a second `bin` entry in `package.json` (`documcp-cli`), keeping `documcp` as the MCP server entry point.
2. Subcommands mirror tool names:

   ```bash
   documcp-cli analyze <path>
   documcp-cli recommend <path>
   documcp-cli setup <path> --ssg <name>
   documcp-cli deploy <path>
   documcp-cli validate <path>
   ```

3. Each subcommand accepts the same parameters as its corresponding MCP tool, validated by the same Zod schemas.
4. Output defaults to human-readable text on stdout; `--json` toggles structured JSON for scripting.
5. Exit codes follow Unix conventions: `0` success, `1` user error, `2` system error.
6. The CLI does **not** duplicate any business logic. It is purely a different transport for the existing tool handlers, enforced by a single shared call site.

## The CLI does **not** duplicate any business logic. It is purely a different transport for the existing tool handlers, enforced by a single shared call site.

<!-- task-id: adr-0017-standalone-cli-surface-for-non-mcp-workflows-md/the-cli-does-not-duplicate-any-business-logic-it-is-purely-a-different-transport/production -->
<!-- adr: adr-0017-standalone-cli-surface-for-non-mcp-workflows.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#126

Add `src/cli.ts` as a thin dispatcher that uses an internal `runTool(name, input)` adapter to call existing tool handlers directly, bypassing MCP request/response framing.

1. Expose this as a second `bin` entry in `package.json` (`documcp-cli`), keeping `documcp` as the MCP server entry point.
2. Subcommands mirror tool names:

   ```bash
   documcp-cli analyze <path>
   documcp-cli recommend <path>
   documcp-cli setup <path> --ssg <name>
   documcp-cli deploy <path>
   documcp-cli validate <path>
   ```

3. Each subcommand accepts the same parameters as its corresponding MCP tool, validated by the same Zod schemas.
4. Output defaults to human-readable text on stdout; `--json` toggles structured JSON for scripting.
5. Exit codes follow Unix conventions: `0` success, `1` user error, `2` system error.
6. The CLI does **not** duplicate any business logic. It is purely a different transport for the existing tool handlers, enforced by a single shared call site.

## Write tests for: **`metadata: TargetMetadata`** — human-readable name, slug, supported SSGs, and capability flags.

<!-- task-id: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloud/metadata-targetmetadata-human-readable-name-slug-supported-ssgs-and-capability-f/test -->
<!-- adr: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloudflare-pages.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#127, tosin2013/documcp#128, tosin2013/documcp#129, tosin2013/documcp#130

Define a `DeployTargetAdapter` interface in `src/deploy-targets/types.ts` mirroring the `SSGAdapter` shape from ADR-016:

```ts
export interface DeployTargetAdapter {
  metadata: TargetMetadata;
  generateDeploymentArtifact(ssg: string, opts: DeployOptions): GeneratedFile[];
  optionalCliCommand(opts: DeployOptions): string | null;
  verify(deployedUrl: string): Promise<VerificationResult>;
}
```

1. **`metadata: TargetMetadata`** — human-readable name, slug, supported SSGs, and capability flags.
2. **`generateDeploymentArtifact(ssg, opts) -> GeneratedFile[]`** — emits one or more files appropriate to the target (e.g., `vercel.json`, `netlify.toml`, `wrangler.toml`, `.github/workflows/deploy.yml`).
3. **`optionalCliCommand(opts) -> string | null`** — returns a platform CLI command (e.g., `vercel deploy --prod`, `netlify deploy --prod`) the user can opt into running.
4. **`verify(deployedUrl) -> VerificationResult`** — reuses the existing URL+200 check from `deploy_pages`.

Concrete adapters live in `src/deploy-targets/<target>/` (`github-pages`, `vercel`, `netlify`, `cloudflare-pages`).

**Adopt MEDIUM DEPTH**: documcp generates the config and can optionally invoke the platform CLI when the user opts in via a tool param, but does **NOT** call platform REST APIs. No API tokens, no new env-var burden in the same release that just removed `DOCUMCP_LLM_*` env vars per [ADR-014](./adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md).

**Rollout sequence**:

- **Vercel** ships as the first non-GitHub-Pages adapter (issue D1, `priority/high`).
- **Netlify** (D2) and **Cloudflare Pages** (D3) are filed as `help wanted` for the community.
- The `deploy_pages` tool is renamed to `deploy_site` with a `target` parameter; a back-compat shim keeps `deploy_pages` working through one minor version (issue D4, `breaking-change`).

## **`metadata: TargetMetadata`** — human-readable name, slug, supported SSGs, and capability flags.

<!-- task-id: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloud/metadata-targetmetadata-human-readable-name-slug-supported-ssgs-and-capability-f/production -->
<!-- adr: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloudflare-pages.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#127, tosin2013/documcp#128, tosin2013/documcp#129, tosin2013/documcp#130

Define a `DeployTargetAdapter` interface in `src/deploy-targets/types.ts` mirroring the `SSGAdapter` shape from ADR-016:

```ts
export interface DeployTargetAdapter {
  metadata: TargetMetadata;
  generateDeploymentArtifact(ssg: string, opts: DeployOptions): GeneratedFile[];
  optionalCliCommand(opts: DeployOptions): string | null;
  verify(deployedUrl: string): Promise<VerificationResult>;
}
```

1. **`metadata: TargetMetadata`** — human-readable name, slug, supported SSGs, and capability flags.
2. **`generateDeploymentArtifact(ssg, opts) -> GeneratedFile[]`** — emits one or more files appropriate to the target (e.g., `vercel.json`, `netlify.toml`, `wrangler.toml`, `.github/workflows/deploy.yml`).
3. **`optionalCliCommand(opts) -> string | null`** — returns a platform CLI command (e.g., `vercel deploy --prod`, `netlify deploy --prod`) the user can opt into running.
4. **`verify(deployedUrl) -> VerificationResult`** — reuses the existing URL+200 check from `deploy_pages`.

Concrete adapters live in `src/deploy-targets/<target>/` (`github-pages`, `vercel`, `netlify`, `cloudflare-pages`).

**Adopt MEDIUM DEPTH**: documcp generates the config and can optionally invoke the platform CLI when the user opts in via a tool param, but does **NOT** call platform REST APIs. No API tokens, no new env-var burden in the same release that just removed `DOCUMCP_LLM_*` env vars per [ADR-014](./adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md).

**Rollout sequence**:

- **Vercel** ships as the first non-GitHub-Pages adapter (issue D1, `priority/high`).
- **Netlify** (D2) and **Cloudflare Pages** (D3) are filed as `help wanted` for the community.
- The `deploy_pages` tool is renamed to `deploy_site` with a `target` parameter; a back-compat shim keeps `deploy_pages` working through one minor version (issue D4, `breaking-change`).

## Write tests for: **`generateDeploymentArtifact(ssg, opts) -> GeneratedFile[]`** — emits one or more files appropriate to the target (e.g., `vercel.json`, `netlify.toml`, `wrangler.toml`, `.github/workflows/deploy.yml`).

<!-- task-id: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloud/generatedeploymentartifact-ssg-opts-generatedfile-emits-one-or-more-files-approp/test -->
<!-- adr: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloudflare-pages.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#127, tosin2013/documcp#128, tosin2013/documcp#129, tosin2013/documcp#130

Define a `DeployTargetAdapter` interface in `src/deploy-targets/types.ts` mirroring the `SSGAdapter` shape from ADR-016:

```ts
export interface DeployTargetAdapter {
  metadata: TargetMetadata;
  generateDeploymentArtifact(ssg: string, opts: DeployOptions): GeneratedFile[];
  optionalCliCommand(opts: DeployOptions): string | null;
  verify(deployedUrl: string): Promise<VerificationResult>;
}
```

1. **`metadata: TargetMetadata`** — human-readable name, slug, supported SSGs, and capability flags.
2. **`generateDeploymentArtifact(ssg, opts) -> GeneratedFile[]`** — emits one or more files appropriate to the target (e.g., `vercel.json`, `netlify.toml`, `wrangler.toml`, `.github/workflows/deploy.yml`).
3. **`optionalCliCommand(opts) -> string | null`** — returns a platform CLI command (e.g., `vercel deploy --prod`, `netlify deploy --prod`) the user can opt into running.
4. **`verify(deployedUrl) -> VerificationResult`** — reuses the existing URL+200 check from `deploy_pages`.

Concrete adapters live in `src/deploy-targets/<target>/` (`github-pages`, `vercel`, `netlify`, `cloudflare-pages`).

**Adopt MEDIUM DEPTH**: documcp generates the config and can optionally invoke the platform CLI when the user opts in via a tool param, but does **NOT** call platform REST APIs. No API tokens, no new env-var burden in the same release that just removed `DOCUMCP_LLM_*` env vars per [ADR-014](./adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md).

**Rollout sequence**:

- **Vercel** ships as the first non-GitHub-Pages adapter (issue D1, `priority/high`).
- **Netlify** (D2) and **Cloudflare Pages** (D3) are filed as `help wanted` for the community.
- The `deploy_pages` tool is renamed to `deploy_site` with a `target` parameter; a back-compat shim keeps `deploy_pages` working through one minor version (issue D4, `breaking-change`).

## **`generateDeploymentArtifact(ssg, opts) -> GeneratedFile[]`** — emits one or more files appropriate to the target (e.g., `vercel.json`, `netlify.toml`, `wrangler.toml`, `.github/workflows/deploy.yml`).

<!-- task-id: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloud/generatedeploymentartifact-ssg-opts-generatedfile-emits-one-or-more-files-approp/production -->
<!-- adr: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloudflare-pages.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#127, tosin2013/documcp#128, tosin2013/documcp#129, tosin2013/documcp#130

Define a `DeployTargetAdapter` interface in `src/deploy-targets/types.ts` mirroring the `SSGAdapter` shape from ADR-016:

```ts
export interface DeployTargetAdapter {
  metadata: TargetMetadata;
  generateDeploymentArtifact(ssg: string, opts: DeployOptions): GeneratedFile[];
  optionalCliCommand(opts: DeployOptions): string | null;
  verify(deployedUrl: string): Promise<VerificationResult>;
}
```

1. **`metadata: TargetMetadata`** — human-readable name, slug, supported SSGs, and capability flags.
2. **`generateDeploymentArtifact(ssg, opts) -> GeneratedFile[]`** — emits one or more files appropriate to the target (e.g., `vercel.json`, `netlify.toml`, `wrangler.toml`, `.github/workflows/deploy.yml`).
3. **`optionalCliCommand(opts) -> string | null`** — returns a platform CLI command (e.g., `vercel deploy --prod`, `netlify deploy --prod`) the user can opt into running.
4. **`verify(deployedUrl) -> VerificationResult`** — reuses the existing URL+200 check from `deploy_pages`.

Concrete adapters live in `src/deploy-targets/<target>/` (`github-pages`, `vercel`, `netlify`, `cloudflare-pages`).

**Adopt MEDIUM DEPTH**: documcp generates the config and can optionally invoke the platform CLI when the user opts in via a tool param, but does **NOT** call platform REST APIs. No API tokens, no new env-var burden in the same release that just removed `DOCUMCP_LLM_*` env vars per [ADR-014](./adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md).

**Rollout sequence**:

- **Vercel** ships as the first non-GitHub-Pages adapter (issue D1, `priority/high`).
- **Netlify** (D2) and **Cloudflare Pages** (D3) are filed as `help wanted` for the community.
- The `deploy_pages` tool is renamed to `deploy_site` with a `target` parameter; a back-compat shim keeps `deploy_pages` working through one minor version (issue D4, `breaking-change`).

## Write tests for: **`optionalCliCommand(opts) -> string | null`** — returns a platform CLI command (e.g., `vercel deploy --prod`, `netlify deploy --prod`) the user can opt into running.

<!-- task-id: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloud/optionalclicommand-opts-string-null-returns-a-platform-cli-command-e-g-vercel-de/test -->
<!-- adr: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloudflare-pages.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#127, tosin2013/documcp#128, tosin2013/documcp#129, tosin2013/documcp#130

Define a `DeployTargetAdapter` interface in `src/deploy-targets/types.ts` mirroring the `SSGAdapter` shape from ADR-016:

```ts
export interface DeployTargetAdapter {
  metadata: TargetMetadata;
  generateDeploymentArtifact(ssg: string, opts: DeployOptions): GeneratedFile[];
  optionalCliCommand(opts: DeployOptions): string | null;
  verify(deployedUrl: string): Promise<VerificationResult>;
}
```

1. **`metadata: TargetMetadata`** — human-readable name, slug, supported SSGs, and capability flags.
2. **`generateDeploymentArtifact(ssg, opts) -> GeneratedFile[]`** — emits one or more files appropriate to the target (e.g., `vercel.json`, `netlify.toml`, `wrangler.toml`, `.github/workflows/deploy.yml`).
3. **`optionalCliCommand(opts) -> string | null`** — returns a platform CLI command (e.g., `vercel deploy --prod`, `netlify deploy --prod`) the user can opt into running.
4. **`verify(deployedUrl) -> VerificationResult`** — reuses the existing URL+200 check from `deploy_pages`.

Concrete adapters live in `src/deploy-targets/<target>/` (`github-pages`, `vercel`, `netlify`, `cloudflare-pages`).

**Adopt MEDIUM DEPTH**: documcp generates the config and can optionally invoke the platform CLI when the user opts in via a tool param, but does **NOT** call platform REST APIs. No API tokens, no new env-var burden in the same release that just removed `DOCUMCP_LLM_*` env vars per [ADR-014](./adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md).

**Rollout sequence**:

- **Vercel** ships as the first non-GitHub-Pages adapter (issue D1, `priority/high`).
- **Netlify** (D2) and **Cloudflare Pages** (D3) are filed as `help wanted` for the community.
- The `deploy_pages` tool is renamed to `deploy_site` with a `target` parameter; a back-compat shim keeps `deploy_pages` working through one minor version (issue D4, `breaking-change`).

## **`optionalCliCommand(opts) -> string | null`** — returns a platform CLI command (e.g., `vercel deploy --prod`, `netlify deploy --prod`) the user can opt into running.

<!-- task-id: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloud/optionalclicommand-opts-string-null-returns-a-platform-cli-command-e-g-vercel-de/production -->
<!-- adr: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloudflare-pages.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#127, tosin2013/documcp#128, tosin2013/documcp#129, tosin2013/documcp#130

Define a `DeployTargetAdapter` interface in `src/deploy-targets/types.ts` mirroring the `SSGAdapter` shape from ADR-016:

```ts
export interface DeployTargetAdapter {
  metadata: TargetMetadata;
  generateDeploymentArtifact(ssg: string, opts: DeployOptions): GeneratedFile[];
  optionalCliCommand(opts: DeployOptions): string | null;
  verify(deployedUrl: string): Promise<VerificationResult>;
}
```

1. **`metadata: TargetMetadata`** — human-readable name, slug, supported SSGs, and capability flags.
2. **`generateDeploymentArtifact(ssg, opts) -> GeneratedFile[]`** — emits one or more files appropriate to the target (e.g., `vercel.json`, `netlify.toml`, `wrangler.toml`, `.github/workflows/deploy.yml`).
3. **`optionalCliCommand(opts) -> string | null`** — returns a platform CLI command (e.g., `vercel deploy --prod`, `netlify deploy --prod`) the user can opt into running.
4. **`verify(deployedUrl) -> VerificationResult`** — reuses the existing URL+200 check from `deploy_pages`.

Concrete adapters live in `src/deploy-targets/<target>/` (`github-pages`, `vercel`, `netlify`, `cloudflare-pages`).

**Adopt MEDIUM DEPTH**: documcp generates the config and can optionally invoke the platform CLI when the user opts in via a tool param, but does **NOT** call platform REST APIs. No API tokens, no new env-var burden in the same release that just removed `DOCUMCP_LLM_*` env vars per [ADR-014](./adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md).

**Rollout sequence**:

- **Vercel** ships as the first non-GitHub-Pages adapter (issue D1, `priority/high`).
- **Netlify** (D2) and **Cloudflare Pages** (D3) are filed as `help wanted` for the community.
- The `deploy_pages` tool is renamed to `deploy_site` with a `target` parameter; a back-compat shim keeps `deploy_pages` working through one minor version (issue D4, `breaking-change`).

## Write tests for: **`verify(deployedUrl) -> VerificationResult`** — reuses the existing URL+200 check from `deploy_pages`.

<!-- task-id: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloud/verify-deployedurl-verificationresult-reuses-the-existing-url-200-check-from-dep/test -->
<!-- adr: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloudflare-pages.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#127, tosin2013/documcp#128, tosin2013/documcp#129, tosin2013/documcp#130

Define a `DeployTargetAdapter` interface in `src/deploy-targets/types.ts` mirroring the `SSGAdapter` shape from ADR-016:

```ts
export interface DeployTargetAdapter {
  metadata: TargetMetadata;
  generateDeploymentArtifact(ssg: string, opts: DeployOptions): GeneratedFile[];
  optionalCliCommand(opts: DeployOptions): string | null;
  verify(deployedUrl: string): Promise<VerificationResult>;
}
```

1. **`metadata: TargetMetadata`** — human-readable name, slug, supported SSGs, and capability flags.
2. **`generateDeploymentArtifact(ssg, opts) -> GeneratedFile[]`** — emits one or more files appropriate to the target (e.g., `vercel.json`, `netlify.toml`, `wrangler.toml`, `.github/workflows/deploy.yml`).
3. **`optionalCliCommand(opts) -> string | null`** — returns a platform CLI command (e.g., `vercel deploy --prod`, `netlify deploy --prod`) the user can opt into running.
4. **`verify(deployedUrl) -> VerificationResult`** — reuses the existing URL+200 check from `deploy_pages`.

Concrete adapters live in `src/deploy-targets/<target>/` (`github-pages`, `vercel`, `netlify`, `cloudflare-pages`).

**Adopt MEDIUM DEPTH**: documcp generates the config and can optionally invoke the platform CLI when the user opts in via a tool param, but does **NOT** call platform REST APIs. No API tokens, no new env-var burden in the same release that just removed `DOCUMCP_LLM_*` env vars per [ADR-014](./adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md).

**Rollout sequence**:

- **Vercel** ships as the first non-GitHub-Pages adapter (issue D1, `priority/high`).
- **Netlify** (D2) and **Cloudflare Pages** (D3) are filed as `help wanted` for the community.
- The `deploy_pages` tool is renamed to `deploy_site` with a `target` parameter; a back-compat shim keeps `deploy_pages` working through one minor version (issue D4, `breaking-change`).

## **`verify(deployedUrl) -> VerificationResult`** — reuses the existing URL+200 check from `deploy_pages`.

<!-- task-id: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloud/verify-deployedurl-verificationresult-reuses-the-existing-url-200-check-from-dep/production -->
<!-- adr: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloudflare-pages.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#127, tosin2013/documcp#128, tosin2013/documcp#129, tosin2013/documcp#130

Define a `DeployTargetAdapter` interface in `src/deploy-targets/types.ts` mirroring the `SSGAdapter` shape from ADR-016:

```ts
export interface DeployTargetAdapter {
  metadata: TargetMetadata;
  generateDeploymentArtifact(ssg: string, opts: DeployOptions): GeneratedFile[];
  optionalCliCommand(opts: DeployOptions): string | null;
  verify(deployedUrl: string): Promise<VerificationResult>;
}
```

1. **`metadata: TargetMetadata`** — human-readable name, slug, supported SSGs, and capability flags.
2. **`generateDeploymentArtifact(ssg, opts) -> GeneratedFile[]`** — emits one or more files appropriate to the target (e.g., `vercel.json`, `netlify.toml`, `wrangler.toml`, `.github/workflows/deploy.yml`).
3. **`optionalCliCommand(opts) -> string | null`** — returns a platform CLI command (e.g., `vercel deploy --prod`, `netlify deploy --prod`) the user can opt into running.
4. **`verify(deployedUrl) -> VerificationResult`** — reuses the existing URL+200 check from `deploy_pages`.

Concrete adapters live in `src/deploy-targets/<target>/` (`github-pages`, `vercel`, `netlify`, `cloudflare-pages`).

**Adopt MEDIUM DEPTH**: documcp generates the config and can optionally invoke the platform CLI when the user opts in via a tool param, but does **NOT** call platform REST APIs. No API tokens, no new env-var burden in the same release that just removed `DOCUMCP_LLM_*` env vars per [ADR-014](./adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md).

**Rollout sequence**:

- **Vercel** ships as the first non-GitHub-Pages adapter (issue D1, `priority/high`).
- **Netlify** (D2) and **Cloudflare Pages** (D3) are filed as `help wanted` for the community.
- The `deploy_pages` tool is renamed to `deploy_site` with a `target` parameter; a back-compat shim keeps `deploy_pages` working through one minor version (issue D4, `breaking-change`).

## Write tests for: **Vercel** ships as the first non-GitHub-Pages adapter (issue D1, `priority/high`).

<!-- task-id: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloud/vercel-ships-as-the-first-non-github-pages-adapter-issue-d1-priority-high/test -->
<!-- adr: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloudflare-pages.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#127, tosin2013/documcp#128, tosin2013/documcp#129, tosin2013/documcp#130

Define a `DeployTargetAdapter` interface in `src/deploy-targets/types.ts` mirroring the `SSGAdapter` shape from ADR-016:

```ts
export interface DeployTargetAdapter {
  metadata: TargetMetadata;
  generateDeploymentArtifact(ssg: string, opts: DeployOptions): GeneratedFile[];
  optionalCliCommand(opts: DeployOptions): string | null;
  verify(deployedUrl: string): Promise<VerificationResult>;
}
```

1. **`metadata: TargetMetadata`** — human-readable name, slug, supported SSGs, and capability flags.
2. **`generateDeploymentArtifact(ssg, opts) -> GeneratedFile[]`** — emits one or more files appropriate to the target (e.g., `vercel.json`, `netlify.toml`, `wrangler.toml`, `.github/workflows/deploy.yml`).
3. **`optionalCliCommand(opts) -> string | null`** — returns a platform CLI command (e.g., `vercel deploy --prod`, `netlify deploy --prod`) the user can opt into running.
4. **`verify(deployedUrl) -> VerificationResult`** — reuses the existing URL+200 check from `deploy_pages`.

Concrete adapters live in `src/deploy-targets/<target>/` (`github-pages`, `vercel`, `netlify`, `cloudflare-pages`).

**Adopt MEDIUM DEPTH**: documcp generates the config and can optionally invoke the platform CLI when the user opts in via a tool param, but does **NOT** call platform REST APIs. No API tokens, no new env-var burden in the same release that just removed `DOCUMCP_LLM_*` env vars per [ADR-014](./adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md).

**Rollout sequence**:

- **Vercel** ships as the first non-GitHub-Pages adapter (issue D1, `priority/high`).
- **Netlify** (D2) and **Cloudflare Pages** (D3) are filed as `help wanted` for the community.
- The `deploy_pages` tool is renamed to `deploy_site` with a `target` parameter; a back-compat shim keeps `deploy_pages` working through one minor version (issue D4, `breaking-change`).

## **Vercel** ships as the first non-GitHub-Pages adapter (issue D1, `priority/high`).

<!-- task-id: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloud/vercel-ships-as-the-first-non-github-pages-adapter-issue-d1-priority-high/production -->
<!-- adr: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloudflare-pages.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#127, tosin2013/documcp#128, tosin2013/documcp#129, tosin2013/documcp#130

Define a `DeployTargetAdapter` interface in `src/deploy-targets/types.ts` mirroring the `SSGAdapter` shape from ADR-016:

```ts
export interface DeployTargetAdapter {
  metadata: TargetMetadata;
  generateDeploymentArtifact(ssg: string, opts: DeployOptions): GeneratedFile[];
  optionalCliCommand(opts: DeployOptions): string | null;
  verify(deployedUrl: string): Promise<VerificationResult>;
}
```

1. **`metadata: TargetMetadata`** — human-readable name, slug, supported SSGs, and capability flags.
2. **`generateDeploymentArtifact(ssg, opts) -> GeneratedFile[]`** — emits one or more files appropriate to the target (e.g., `vercel.json`, `netlify.toml`, `wrangler.toml`, `.github/workflows/deploy.yml`).
3. **`optionalCliCommand(opts) -> string | null`** — returns a platform CLI command (e.g., `vercel deploy --prod`, `netlify deploy --prod`) the user can opt into running.
4. **`verify(deployedUrl) -> VerificationResult`** — reuses the existing URL+200 check from `deploy_pages`.

Concrete adapters live in `src/deploy-targets/<target>/` (`github-pages`, `vercel`, `netlify`, `cloudflare-pages`).

**Adopt MEDIUM DEPTH**: documcp generates the config and can optionally invoke the platform CLI when the user opts in via a tool param, but does **NOT** call platform REST APIs. No API tokens, no new env-var burden in the same release that just removed `DOCUMCP_LLM_*` env vars per [ADR-014](./adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md).

**Rollout sequence**:

- **Vercel** ships as the first non-GitHub-Pages adapter (issue D1, `priority/high`).
- **Netlify** (D2) and **Cloudflare Pages** (D3) are filed as `help wanted` for the community.
- The `deploy_pages` tool is renamed to `deploy_site` with a `target` parameter; a back-compat shim keeps `deploy_pages` working through one minor version (issue D4, `breaking-change`).

## Write tests for: **Netlify** (D2) and **Cloudflare Pages** (D3) are filed as `help wanted` for the community.

<!-- task-id: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloud/netlify-d2-and-cloudflare-pages-d3-are-filed-as-help-wanted-for-the-community/test -->
<!-- adr: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloudflare-pages.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#127, tosin2013/documcp#128, tosin2013/documcp#129, tosin2013/documcp#130

Define a `DeployTargetAdapter` interface in `src/deploy-targets/types.ts` mirroring the `SSGAdapter` shape from ADR-016:

```ts
export interface DeployTargetAdapter {
  metadata: TargetMetadata;
  generateDeploymentArtifact(ssg: string, opts: DeployOptions): GeneratedFile[];
  optionalCliCommand(opts: DeployOptions): string | null;
  verify(deployedUrl: string): Promise<VerificationResult>;
}
```

1. **`metadata: TargetMetadata`** — human-readable name, slug, supported SSGs, and capability flags.
2. **`generateDeploymentArtifact(ssg, opts) -> GeneratedFile[]`** — emits one or more files appropriate to the target (e.g., `vercel.json`, `netlify.toml`, `wrangler.toml`, `.github/workflows/deploy.yml`).
3. **`optionalCliCommand(opts) -> string | null`** — returns a platform CLI command (e.g., `vercel deploy --prod`, `netlify deploy --prod`) the user can opt into running.
4. **`verify(deployedUrl) -> VerificationResult`** — reuses the existing URL+200 check from `deploy_pages`.

Concrete adapters live in `src/deploy-targets/<target>/` (`github-pages`, `vercel`, `netlify`, `cloudflare-pages`).

**Adopt MEDIUM DEPTH**: documcp generates the config and can optionally invoke the platform CLI when the user opts in via a tool param, but does **NOT** call platform REST APIs. No API tokens, no new env-var burden in the same release that just removed `DOCUMCP_LLM_*` env vars per [ADR-014](./adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md).

**Rollout sequence**:

- **Vercel** ships as the first non-GitHub-Pages adapter (issue D1, `priority/high`).
- **Netlify** (D2) and **Cloudflare Pages** (D3) are filed as `help wanted` for the community.
- The `deploy_pages` tool is renamed to `deploy_site` with a `target` parameter; a back-compat shim keeps `deploy_pages` working through one minor version (issue D4, `breaking-change`).

## **Netlify** (D2) and **Cloudflare Pages** (D3) are filed as `help wanted` for the community.

<!-- task-id: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloud/netlify-d2-and-cloudflare-pages-d3-are-filed-as-help-wanted-for-the-community/production -->
<!-- adr: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloudflare-pages.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#127, tosin2013/documcp#128, tosin2013/documcp#129, tosin2013/documcp#130

Define a `DeployTargetAdapter` interface in `src/deploy-targets/types.ts` mirroring the `SSGAdapter` shape from ADR-016:

```ts
export interface DeployTargetAdapter {
  metadata: TargetMetadata;
  generateDeploymentArtifact(ssg: string, opts: DeployOptions): GeneratedFile[];
  optionalCliCommand(opts: DeployOptions): string | null;
  verify(deployedUrl: string): Promise<VerificationResult>;
}
```

1. **`metadata: TargetMetadata`** — human-readable name, slug, supported SSGs, and capability flags.
2. **`generateDeploymentArtifact(ssg, opts) -> GeneratedFile[]`** — emits one or more files appropriate to the target (e.g., `vercel.json`, `netlify.toml`, `wrangler.toml`, `.github/workflows/deploy.yml`).
3. **`optionalCliCommand(opts) -> string | null`** — returns a platform CLI command (e.g., `vercel deploy --prod`, `netlify deploy --prod`) the user can opt into running.
4. **`verify(deployedUrl) -> VerificationResult`** — reuses the existing URL+200 check from `deploy_pages`.

Concrete adapters live in `src/deploy-targets/<target>/` (`github-pages`, `vercel`, `netlify`, `cloudflare-pages`).

**Adopt MEDIUM DEPTH**: documcp generates the config and can optionally invoke the platform CLI when the user opts in via a tool param, but does **NOT** call platform REST APIs. No API tokens, no new env-var burden in the same release that just removed `DOCUMCP_LLM_*` env vars per [ADR-014](./adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md).

**Rollout sequence**:

- **Vercel** ships as the first non-GitHub-Pages adapter (issue D1, `priority/high`).
- **Netlify** (D2) and **Cloudflare Pages** (D3) are filed as `help wanted` for the community.
- The `deploy_pages` tool is renamed to `deploy_site` with a `target` parameter; a back-compat shim keeps `deploy_pages` working through one minor version (issue D4, `breaking-change`).

## Write tests for: The `deploy_pages` tool is renamed to `deploy_site` with a `target` parameter; a back-compat shim keeps `deploy_pages` working through one minor version (issue D4, `breaking-change`).

<!-- task-id: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloud/the-deploy-pages-tool-is-renamed-to-deploy-site-with-a-target-parameter-a-back-c/test -->
<!-- adr: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloudflare-pages.md -->
<!-- tdd: test -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#127, tosin2013/documcp#128, tosin2013/documcp#129, tosin2013/documcp#130

Define a `DeployTargetAdapter` interface in `src/deploy-targets/types.ts` mirroring the `SSGAdapter` shape from ADR-016:

```ts
export interface DeployTargetAdapter {
  metadata: TargetMetadata;
  generateDeploymentArtifact(ssg: string, opts: DeployOptions): GeneratedFile[];
  optionalCliCommand(opts: DeployOptions): string | null;
  verify(deployedUrl: string): Promise<VerificationResult>;
}
```

1. **`metadata: TargetMetadata`** — human-readable name, slug, supported SSGs, and capability flags.
2. **`generateDeploymentArtifact(ssg, opts) -> GeneratedFile[]`** — emits one or more files appropriate to the target (e.g., `vercel.json`, `netlify.toml`, `wrangler.toml`, `.github/workflows/deploy.yml`).
3. **`optionalCliCommand(opts) -> string | null`** — returns a platform CLI command (e.g., `vercel deploy --prod`, `netlify deploy --prod`) the user can opt into running.
4. **`verify(deployedUrl) -> VerificationResult`** — reuses the existing URL+200 check from `deploy_pages`.

Concrete adapters live in `src/deploy-targets/<target>/` (`github-pages`, `vercel`, `netlify`, `cloudflare-pages`).

**Adopt MEDIUM DEPTH**: documcp generates the config and can optionally invoke the platform CLI when the user opts in via a tool param, but does **NOT** call platform REST APIs. No API tokens, no new env-var burden in the same release that just removed `DOCUMCP_LLM_*` env vars per [ADR-014](./adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md).

**Rollout sequence**:

- **Vercel** ships as the first non-GitHub-Pages adapter (issue D1, `priority/high`).
- **Netlify** (D2) and **Cloudflare Pages** (D3) are filed as `help wanted` for the community.
- The `deploy_pages` tool is renamed to `deploy_site` with a `target` parameter; a back-compat shim keeps `deploy_pages` working through one minor version (issue D4, `breaking-change`).

## The `deploy_pages` tool is renamed to `deploy_site` with a `target` parameter; a back-compat shim keeps `deploy_pages` working through one minor version (issue D4, `breaking-change`).

<!-- task-id: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloud/the-deploy-pages-tool-is-renamed-to-deploy-site-with-a-target-parameter-a-back-c/production -->
<!-- adr: adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloudflare-pages.md -->
<!-- tdd: production -->

**Status:** pending
**Priority:** high
**Related issues:** tosin2013/documcp#127, tosin2013/documcp#128, tosin2013/documcp#129, tosin2013/documcp#130

Define a `DeployTargetAdapter` interface in `src/deploy-targets/types.ts` mirroring the `SSGAdapter` shape from ADR-016:

```ts
export interface DeployTargetAdapter {
  metadata: TargetMetadata;
  generateDeploymentArtifact(ssg: string, opts: DeployOptions): GeneratedFile[];
  optionalCliCommand(opts: DeployOptions): string | null;
  verify(deployedUrl: string): Promise<VerificationResult>;
}
```

1. **`metadata: TargetMetadata`** — human-readable name, slug, supported SSGs, and capability flags.
2. **`generateDeploymentArtifact(ssg, opts) -> GeneratedFile[]`** — emits one or more files appropriate to the target (e.g., `vercel.json`, `netlify.toml`, `wrangler.toml`, `.github/workflows/deploy.yml`).
3. **`optionalCliCommand(opts) -> string | null`** — returns a platform CLI command (e.g., `vercel deploy --prod`, `netlify deploy --prod`) the user can opt into running.
4. **`verify(deployedUrl) -> VerificationResult`** — reuses the existing URL+200 check from `deploy_pages`.

Concrete adapters live in `src/deploy-targets/<target>/` (`github-pages`, `vercel`, `netlify`, `cloudflare-pages`).

**Adopt MEDIUM DEPTH**: documcp generates the config and can optionally invoke the platform CLI when the user opts in via a tool param, but does **NOT** call platform REST APIs. No API tokens, no new env-var burden in the same release that just removed `DOCUMCP_LLM_*` env vars per [ADR-014](./adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md).

**Rollout sequence**:

- **Vercel** ships as the first non-GitHub-Pages adapter (issue D1, `priority/high`).
- **Netlify** (D2) and **Cloudflare Pages** (D3) are filed as `help wanted` for the community.
- The `deploy_pages` tool is renamed to `deploy_site` with a `target` parameter; a back-compat shim keeps `deploy_pages` working through one minor version (issue D4, `breaking-change`).

<!-- /ADR-GENERATED-TASKS -->
