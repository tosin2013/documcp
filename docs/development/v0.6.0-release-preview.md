---
id: v0-6-0-release-preview
title: "v0.6.0 — LLM Removal: Release Preview"
sidebar_label: "v0.6.0 Release Preview"
sidebar_position: 60
---

# v0.6.0 — LLM Removal: Release Preview

> **Status (2026-05-01): Merged to `main` at [`3f1d691b`](https://github.com/tosin2013/documcp/commit/3f1d691b); Tag and Publish Pending.**
> Generated by `release_tracking next_release_preview` and refined for human readers. The full migration content below is preserved as the canonical reference. Skip to the [publish runbook at the end](#when-youre-ready-to-publish-v060) for the exact tag-and-ship command sequence.

## TL;DR

DocuMCP v0.6.0 is a **breaking release** that removes the bundled multi-provider LLM client and aligns the server with MCP best practice: LLM execution belongs in the host, not the server. Hybrid semantic analysis is replaced by deterministic AST analysis; future LLM-driven features will use **MCP Sampling**.

**Driving ADR**: [ADR-014: Removal of Server-Side LLM Dependency in Favor of MCP Sampling](../adrs/adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md). Supersedes Phase 2.5 of [ADR-009](../adrs/adr-0009-content-accuracy-validation-framework.md).

**Suggested version bump**: minor → `0.5.10` → `0.6.0` (semver minor for breaking changes pre-1.0; tagged `BREAKING CHANGE:` in CHANGELOG).

## Release Readiness

| Check                                            | Status                                                                             |
| ------------------------------------------------ | ---------------------------------------------------------------------------------- |
| Driving ADRs accepted                            | ✅ ADRs 014–018 accepted 2026-04-30                                                |
| Tracked issues open                              | 5 (issues #106–#110)                                                               |
| Working tree clean                               | ⚠️ Uncommitted ADR/RELEASE_PLAN changes — commit before tag                        |
| Tests passing                                    | ⏳ Run `npm run ci` before tagging                                                 |
| `npm run docs:check-links`                       | ⏳ Run before tagging                                                              |
| CHANGELOG updated with `BREAKING CHANGE:` footer | ⏳ See [CHANGELOG.md](https://github.com/tosin2013/documcp/blob/main/CHANGELOG.md) |

**Overall**: ❌ Not ready until tracked issues are closed and CI is green. Score 90% (per `release_tracking`); blockers are work-in-progress code and unrun tests.

## Pending ADRs (Accepted, Not Yet Released)

These ADRs were accepted in this release cycle and ship together where applicable:

| ADR                                                                                                               | Title                                                           | Releases In |
| ----------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ----------- |
| [ADR-014](../adrs/adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md)                     | Removal of Server-Side LLM Dependency in Favor of MCP Sampling  | **v0.6.0**  |
| [ADR-015](../adrs/adr-0015-multi-language-ast-analysis-via-tree-sitter-parser-registry.md)                        | Multi-Language AST Analysis via Tree-sitter Parser Registry     | v0.7.0      |
| [ADR-016](../adrs/adr-0016-ssg-adapter-plugin-pattern-for-recommendation-configuration-and-deployment.md)         | SSG Adapter Plugin Pattern                                      | v1.0.0      |
| [ADR-017](../adrs/adr-0017-standalone-cli-surface-for-non-mcp-workflows.md)                                       | Standalone CLI Surface for Non-MCP Workflows                    | v1.0.0      |
| [ADR-018](../adrs/adr-0018-deploy-target-adapter-pattern-for-github-pages-vercel-netlify-and-cloudflare-pages.md) | Deploy Target Adapter Pattern (Vercel/Netlify/Cloudflare Pages) | v1.0.0      |

Only **ADR-014** ships in v0.6.0. The others are listed here because they were accepted in the same review cycle and contributors may see them in `git log`.

## Tracked Issues for v0.6.0

[Milestone v0.6.0 — LLM Removal](https://github.com/tosin2013/documcp/milestone/1):

- [#106 [Epic] Remove server-side LLM dependency from DocuMCP](https://github.com/tosin2013/documcp/issues/106)
- [#107 Delete LLM client and provider configuration](https://github.com/tosin2013/documcp/issues/107) — `priority/high`
- [#108 Refactor semantic-analyzer to AST-only mode](https://github.com/tosin2013/documcp/issues/108)
- [#109 Remove simulate_execution tool and execution-simulator utility](https://github.com/tosin2013/documcp/issues/109)
- [#110 Update ADR-009 and remove LLM how-to documentation](https://github.com/tosin2013/documcp/issues/110)

All five are required for v0.6.0 GA. None are blocked.

## Breaking Changes

### Removed APIs / tools

- **`simulate_execution` MCP tool** — gone. Hosts that called `tools/call simulate_execution` will get a "tool not found" error. There is no AST-only equivalent; remove the call from your host config.
- **`SemanticAnalyzer.useLLM` / `SemanticAnalyzer.confidenceThreshold` / `SemanticAnalyzer.llmConfig`** — gone. The `createSemanticAnalyzer()` factory now ignores these options (and warns on first call before deletion in v0.7.0).
- **`LLMClient` class** (`src/utils/llm-client.ts`) — gone. External orchestrators that imported `LLMClient` from `documcp` must vendor their own client or migrate to MCP Sampling.

### Removed environment variables

- `DOCUMCP_LLM_PROVIDER`
- `DOCUMCP_LLM_API_KEY`
- `DOCUMCP_LLM_MODEL`
- `DOCUMCP_LLM_BASE_URL`

These are silently ignored in v0.6.0 (with a one-time stderr deprecation warning) and removed entirely in v0.7.0.

### Removed documentation

- `docs/how-to/llm-integration.md` — deleted.

## Migration Guide

### If you used hybrid mode

You don't have to do anything for AST-only mode to keep working — that is now the default and only mode. If you specifically relied on LLM-grade semantic detection (e.g., your CI fails when an API doc is "about right" but an enum value changed semantically), one of two paths:

1. **Wait for MCP Sampling** — when host coverage broadens, DocuMCP will issue `sampling/createMessage` requests through the host, using the host's LLM and policies. No env vars; no separate cost stream.
2. **Move the LLM call to your orchestrator** — call your LLM provider yourself with the same prompt that DocuMCP previously used, then feed the result into `validate_content` as a hint.

### If you imported `LLMClient` programmatically

Vendor the file from the v0.5.x tag if you really need it; it will not be re-exported in v0.6.0. The recommended path is to migrate to MCP Sampling client-side.

## Unreleased Changes Since v0.5.10

```text
c4270bf docs(adrs): update implementation status for ADRs 003, 004, 006, 009, 011
d671cec docs: update ADR-008 and ADR-007 status to fully implemented
```

Plus the ADR-014–018 additions and the `area/deploy-target` issue family (#127–#130, all targeting v1.0.0, not v0.6.0).

## Pre-Release Checklist

Run, in order:

1. Close all five v0.6.0 issues (`#106–#110`).
2. `npm run typecheck && npm run lint && npm run test:ci && npm run build`.
3. `npm run docs:check-links` — must pass.
4. `npm run docs:validate` — must pass.
5. Update [CHANGELOG.md](https://github.com/tosin2013/documcp/blob/main/CHANGELOG.md) `## [Unreleased]` → `## [0.6.0] - YYYY-MM-DD` with the `BREAKING CHANGE:` footer (see the changelog section that follows for the template).
6. `npm run release:minor` (will run `standard-version`, tag, and update `package.json`).
7. Push tag and let the publish workflow run. Verify on npmjs.org.
8. Open the [v1.0.0 — Ecosystem milestone](https://github.com/tosin2013/documcp/milestone/4) and start D1 (Vercel adapter, [#127](https://github.com/tosin2013/documcp/issues/127)) as the next batch.

## Confidence

| Dimension       | Score                                                                                                                  |
| --------------- | ---------------------------------------------------------------------------------------------------------------------- |
| ADR coverage    | 100% (every breaking change has an ADR)                                                                                |
| Test plan       | 80% (existing 80% coverage gate covers the deletions; new contract tests come with semantic-analyzer refactor in #108) |
| Migration story | 95% (every removed surface has a migration paragraph above)                                                            |

Risk acknowledged in [ADR-014 §Risks](../adrs/adr-0014-removal-of-server-side-llm-dependency-in-favor-of-mcp-sampling.md#risks-and-mitigations): external orchestrators that import `LLMClient` will break; mitigated by the `BREAKING CHANGE:` tag and the deprecation-warning grace period.

---

## When you're ready to publish v0.6.0

The merge of [PR #139](https://github.com/tosin2013/documcp/pull/139) at commit [`3f1d691b`](https://github.com/tosin2013/documcp/commit/3f1d691b) did **not** publish to npm or create a GitHub Release; the [release pipeline](../../.github/workflows/release.yml) only fires on tag push (`v*.*.*`) or `workflow_dispatch`. When you decide to ship, run these three commands from a clean checkout of `main`:

```bash
git tag -a v0.6.0 3f1d691b -m "Release v0.6.0 — LLM Removal (see CHANGELOG.md and ADR-014)"

git push origin v0.6.0

gh run watch $(gh run list --workflow=release.yml --limit 1 --json databaseId --jq '.[0].databaseId')
```

### What the pipeline does (in order)

1. **Pre-release tests** ([release.yml lines 19-53](../../.github/workflows/release.yml)): full Jest suite with `--coverage`, `npm run test:performance`, `npm run build`. Hard-gated on **85% coverage** at line 43. (Project-wide jest threshold is currently 80%; if the gate fails, see "If pre-release gates fail" below.)
2. **Release** ([release.yml lines 55-325](../../.github/workflows/release.yml)): validates commits since v0.5.10 with commitlint, creates the GitHub Release via `ncipollo/release-action@v1` using the auto-extracted `## [0.6.0]` section of [CHANGELOG.md](https://github.com/tosin2013/documcp/blob/main/CHANGELOG.md), then `npm publish --access public` using **OIDC trusted publishing** (no `NODE_AUTH_TOKEN` needed — see fallback below if not configured).
3. **Docs** ([release.yml lines 327-373](../../.github/workflows/release.yml)): generates API docs, builds Docusaurus, deploys to GitHub Pages.

### If npm trusted publishing isn't configured

If `npm publish` fails with `403` or trusted-publisher errors:

- **A (recommended)**: configure trusted publishing on npmjs.com → Account → Packages → `documcp` → Publishing access → "Add trusted publisher" with `repository = tosin2013/documcp`, `workflow = release.yml`, `environment = (none)`. Re-run the failed `release` job.
- **B (fallback)**: add `NPM_TOKEN` repository secret (granular, write-only on `documcp`), then patch [release.yml](../../.github/workflows/release.yml) around line 254 to inject `env: NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}` to the `npm publish` step. File a follow-up to migrate to OIDC afterward.

### If pre-release gates fail

- **Coverage 85% gate fails**: either (a) raise coverage by adding tests in the affected files, or (b) lower [release.yml line 43](../../.github/workflows/release.yml) `if (( $(echo "$coverage < 85" | bc -l) ))` to `< 80` so the project-wide jest threshold (80%) becomes the source of truth. Open a follow-up issue if you choose (b).
- **Commitlint fails**: typically benign in our case (every commit since v0.5.10 was either a conventional `feat!:` / `ci(deps):` / `fix(deps):` / `chore(release):` or a Copilot-bootstrap auto-squashed via `--admin --squash`). If a real failure shows up, fix the offending commit and force-push to a topic branch, then re-tag.

### 30-second post-publish verification

```bash
npm view documcp@0.6.0 version dist-tags
gh release view v0.6.0 --json name,tagName,publishedAt
gh api repos/tosin2013/documcp/git/refs/tags/v0.6.0 --jq '.object.sha'
```

Expected: `0.6.0` reported, dist-tag `latest = 0.6.0`, GitHub Release exists, tag SHA matches `3f1d691b`.

### Rollback (if the release goes wrong)

| Surface        | Command                                                | Notes                                                                                                                                                                                                                                                                          |
| -------------- | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| GitHub Release | `gh release delete v0.6.0 --yes`                       | Re-creatable.                                                                                                                                                                                                                                                                  |
| Git tag        | `git push --delete origin v0.6.0 && git tag -d v0.6.0` | Re-creatable.                                                                                                                                                                                                                                                                  |
| npm            | `npm deprecate documcp@0.6.0 "<reason>"`               | Recommended path. The 72-hour `npm unpublish documcp@0.6.0` window is a one-shot escape hatch — only use if the package is genuinely broken (security leak, missing files), not for cosmetic issues. After 72 hours, only `deprecate` is available; ship `0.6.1` with the fix. |

### Why this is a separate runbook (not auto-triggered on merge)

PR #139 was an _implementation_ landing, not a _release_ ceremony. Tagging is a deliberate human decision because:

1. npm publishes are **permanent** (can be deprecated but not truly unpublished after 72 hours).
2. GitHub Pages docs deploy will replace the live `latest` docs site.
3. Downstream MCP hosts pinning `documcp@latest` will pick up the breaking changes on their next install.

The cost of "land-on-main" → "tag-and-publish" being separate steps is one manual `git push origin v0.6.0`. The benefit is that you decide when downstream is exposed.
